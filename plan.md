# Launch Readiness — Execution Plan

Source: [`feedback.md`](./feedback.md) (Developer Launch Checklist). This plan reorganizes that checklist into sequenced, actionable workstreams across both repos — `startup_ninja_backend` and `startup_ninja_frontend`. It does not replace `feedback.md`; check items off there as the source of truth for scope, use this to decide **order** and **how**.

**Definition of done** (per `feedback.md`): works on a fresh test account, has loading/success/error states, has a test link or short recording attached.

**Repo tags**: `[BE]` = startup_ninja_backend, `[FE]` = startup_ninja_frontend, `[BE+FE]` = both.

---

## Phase 0 — Foundations ✅ Complete

Fixing these first prevents rework: half the P0 checklist assumes working sessions, a working entitlement check, and shared UI primitives. Every checklist item below is checked off — either fixed and verified, or investigated to a definitive conclusion (confirmed not a bug, or confirmed to need a product decision I can't make unilaterally, each stated explicitly rather than left as a vague TODO). Backend changes verified via `node -c` syntax checks, live boot logs, and an end-to-end Redis session test; frontend changes verified via a clean `tsc --noEmit` and a clean production `vite build`.

### 0.1 Session persistence — fixed and verified `[BE]` ✅
`feedback.md` reports users logged out after ~10-15 min despite a documented 30-min inactivity window. Traced the cause:
- `SESSION_TIMEOUT_MS` (default 30 min) is checked identically in `api-gateway/middleware/auth.js`, root `middleware/auth.js`, and every service's `src/middleware/auth.js` — each does `inactiveTime = Date.now() - user.lastActivity` where `user` is the **decoded JWT payload**.
- The code then does `user.lastActivity = Date.now()` — but this mutates the in-memory decoded object only. **No new token is issued or sent back to the client.** The client keeps resending the original token from login, whose `lastActivity` claim never moves.
- Net effect: this is not a sliding inactivity window at all — it's a fixed timeout measured from login (or from the last token refresh, if refresh also doesn't bump `lastActivity` — needs confirming in `services/auth-service/src/utils/jwt.js`). Any client-side clock drift, multi-tab usage, or a shorter-than-expected refresh cadence would present exactly as "logged out after 10-15 min."

**Fix applied**: `lastActivity` is no longer trusted from the JWT claim. It's now tracked server-side in Redis (key `last_activity:<userId>`, TTL = `SESSION_TIMEOUT_MS`, refreshed on every authenticated request) in the 3 places that actually enforce this check on the live request path: `services/auth-service/src/utils/redisClient.js` + `api-gateway/utils/redisClient.js` (extended with `updateLastActivity`/`getLastActivity`), plus a new minimal `services/ninja-sales-service/src/utils/redisClient.js` (that service didn't have a Redis client at all before). The middlewares in `services/auth-service/src/middleware/auth.js`, `api-gateway/middleware/auth.js`, and `services/ninja-sales-service/src/middleware/auth.js` now check/refresh Redis instead of the static JWT claim, falling back to the token's `iat` only for the very first request after login/refresh (before Redis has a record). Root `middleware/auth.js` and `middleware/tokenBlacklist.js` turned out to be dead code — nothing actually requires them (every service has its own local copy) — left untouched.
- [x] Move `lastActivity` tracking to Redis, in the 3 files that actually enforce it (root `middleware/auth.js` is unused dead code, confirmed via grep for its requires — not worth touching)
- [x] Verified end-to-end: logged in as the seeded admin, called a protected endpoint, confirmed Redis key `last_activity:<userId>` created with ~1800s TTL; then manually set a 31-minutes-stale timestamp and confirmed the middleware correctly returns 401 "Session expired due to inactivity" — genuine sliding window confirmed both ways
- [x] Refresh-token flow (`POST /auth/refresh-token`) confirmed compatible without changes — it's a standalone endpoint (not behind `authenticateToken`), and a freshly refreshed token's `iat` naturally becomes the new Redis baseline on the next authenticated call
- [x] Frontend `apiClient.ts`'s 401 → refresh flow reviewed — already correctly excludes auth endpoints from the retry loop, reads the refresh token from storage, and dispatches `session-expired` only on genuine refresh failure. No bug found, no change needed (this was the reactive design already; it's now correct because the backend timeout is finally real)
- [x] Built a generic `src/hooks/useDraftPersistence.ts` (localStorage-backed, debounced autosave + restore-on-mount + `clearDraft()`) and wired it into the 3 surfaces named above, as they exist today: the Chat prompt input (`pages/User/AIChat.tsx`), the Sales "Add Project" form (`components/ninja-sales/AddProjectModal.tsx`, all 16 fields via one JSON-serialized draft), and the Website Builder "Create Website" modal (`components/web-builder/CreateWebsiteModal.tsx`, name + description). Each clears its draft on successful submit. Website Builder's modal will likely be replaced by Phase 3's wizard rebuild — the hook carries forward regardless, this just means today's surface isn't left with the known gap in the meantime.
- [x] **Real regression found and fixed post-verification**: caught via a live `npm run dev` restart — `api-gateway`'s Redis client connects lazily (`app.listen()` doesn't wait for it), so there's a ~1-2s window right after a fresh restart where an already-open, already-logged-in browser tab's requests hit the auth middleware before Redis is ready. The original fix's fallback (`user.lastActivity` / `iat` from the JWT, i.e. login time) doesn't know about ongoing activity that Redis was correctly tracking — so during that narrow window, a user who'd been logged in a while but continuously active could be spuriously rejected as "expired." The codebase's own existing convention (`checkTokenBlacklist`) is to fail OPEN when Redis is down; this fix didn't follow it. Fixed: `getLastActivity`/`updateLastActivity` in all 3 `redisClient.js` files now let Redis errors propagate instead of swallowing them into `null`/`false`, and all 3 middlewares wrap the check in `try/catch`, failing open (allowing the request through) on any Redis error rather than falling back to a stale timestamp. Verified: an isolated `getLastActivity` call against an unreachable Redis port genuinely throws (confirming the propagation), and the normal path (Redis reachable) still correctly enforces both the happy path (200) and genuine 31-minute staleness (401) exactly as before.

### 0.2 Server-side plan/quota enforcement — audited, one real bug fixed `[BE]`
**Correction**: this already exists and is more complete than assumed when this plan was first written. Every quota-consuming service (`ai-chat`, `imaginative-service`, `website-builder-service`, `social-media-service`, `ninja-sales-service`, `chatbot`, `ai-legal`, `admin-service`) has its own `utils/limitChecker.js` that calls `POST {AUTH_SERVICE_URL}/user/check-limit` (`services/auth-service/src/controllers/subscriptionController.js` `checkUsageLimit`, which both checks against `Subscription.allowedLimits` and atomically increments a Redis counter `usage:{userId}:{feature}` in one call) as a **pre-action gate**, not just for display. No new middleware needed — audited it instead and fixed what was actually broken:
- [x] **Fixed real bug**: `services/auth-service/src/services/usageSyncService.js` (flushes Redis usage counters into MongoDB's `Subscription.usage` every 5 minutes) was reading/writing the wrong Redis key names — 5 of the 14 real usage fields (`ai_post_writer`, `website_creation`, `website_hosting`, `facebook_page_connect`, `web_builder_sessions`) were never synced to MongoDB at all, while it wasted effort syncing 5 orphaned keys (`websites`, `website_sessions`, `social_accounts`, `pager_websites`, `hosted_websites`) that don't exist in the schema. Live enforcement was unaffected (that reads Redis directly), but this explains part of `feedback.md`'s "usage counters and reset calculations" complaint — any admin/dashboard view reading `Subscription.usage` from Mongo was stale/wrong for those 5 features. Rewrote it to sync exactly the 14 fields defined in `models/Subscription.js`'s `usage` schema.
- [x] Checked the `if (token) { checkLimit(...) }` pattern flagged in several controllers (website-builder-service, social-media-service, chatbot) — verified by reading the actual routes: these are all already behind `authenticateToken` middleware, so `req.headers.authorization` is guaranteed present and valid by the time the controller runs. The `if (token)` check is redundant/dead code, not a live bypass — confirmed before treating it as a bug. Left as-is (cosmetic cleanup only, not worth the risk of touching 3 services' worth of controllers for no functional change).
- [x] `imaginative-service`'s `userId === "anonymous"` fallback resolved — confirmed **not reachable**: `/generate` requires `authenticateToken` (`routes/imageRoutes.js:22`), which guarantees a verified `userId` on `req.user` before the controller runs. Same shape of false alarm as the `if (token)` pattern above; dead defensive code, not a live gap.
- [x] Removed the dead `checkLimit` imports from `invoiceController.js` and `proposalController.js` (confirmed genuinely unused, not just missed call sites). Real enforcement for invoices/proposals isn't possible without first adding dedicated `Plan.limits` fields for them — that's a pricing/business decision (what should the limit even be, per plan tier?) outside what I can decide unilaterally; noting it here rather than inventing numbers.
- [x] `web_builder_sessions` investigated — it's fully wired everywhere *except* enforcement (Plan config with real per-tier limits in `seed-plans.js`, tracked in `Subscription.usage`, shown on the admin dashboard, now correctly synced by the 0.2 fix above) but there is no "start a web builder session" endpoint anywhere in `website-builder-service`'s routes to hook a check into — no session concept exists in the code today, only `website_creation` (already enforced) and per-page/asset actions. This is a genuine product-definition gap (what should count as a "session"?), not a missed wiring — flagging for a product decision rather than guessing at semantics.

### 0.3 Shared UI primitives `[FE]`
Audited first — more already existed than assumed:
- [x] Toast system: already exists and is used broadly — `<Toaster />` (react-hot-toast) mounted once globally in `src/App.tsx`, `toast()` called from 51 files. No change needed.
- [x] Confirmation dialog: already exists — `src/components/AlertModal.tsx` is a well-built, typed, reusable confirm dialog (danger/warning/info/success types, loading state, custom icons). Found and fixed 2 places that bypassed it with native `window.confirm()` instead (`pages/Auth/Login.tsx` and `pages/Auth/Register.tsx`'s "cancel email verification" prompt) — both now use `AlertModal` for visual consistency.
- [x] Loading state: already exists — `src/components/LoadingSpinner.tsx` (branded spinner, fullscreen/inline, size variants). No change needed.
- [x] **Built, did not exist before**: `src/components/EmptyState.tsx` and `src/components/ErrorState.tsx` — no shared primitive existed for "no data yet" or "failed to load" states before this; each page would have had to build its own ad hoc version. These match `AlertModal`'s existing dark-theme visual language (same color tokens, rounded corners, border styles) so they drop in consistently. Not yet wired into any existing page — that's for whichever phase touches each page next (e.g. Phase 5 Sales empty states, Phase 1 billing history).
- [x] Design-system audit — ran a real, bounded pass rather than a full page-by-page review (auditing all ~100+ pages/components for pixel-level consistency isn't a Phase 0-sized task; this establishes ground truth and fixes the biggest concrete issue found):
  - **Finding**: a shared `src/components/Button.tsx` already existed but had **zero usages anywhere in the app** (grep for imports of it: 0 hits) — meanwhile there are **588** hand-rolled `<button>` tags across `src/**/*.tsx`, each independently styled.
  - **Why it had zero adoption**: it didn't match the app's actual visual language. It only had 2 bland variants and, critically, its "primary" color used a hover state (`hover:bg-red-700`) that doesn't correspond to the brand's real primary red. Checked `tailwind.config.js`: the brand color token is `primary.red = #E50000`, but newer components like `AlertModal.tsx` use generic Tailwind `red-500`/`red-400` instead — **two different reds are in use across the app** depending which component you look at, exactly the kind of drift `feedback.md` §1 calls out.
  - **Fixed**: rewrote `Button.tsx` to match the dark-theme language already established by `AlertModal`/`EmptyState`/`ErrorState` (same border/hover/opacity patterns), using the correct brand `primary-red` token, and added the variants that pattern actually needs (`primary`/`secondary`/`danger`/`ghost`, 3 sizes, a `loading` state with spinner) so it's genuinely ready to adopt, not just theoretically available.
  - **Deliberately not done**: retrofitting the 588 existing buttons to use it. That's a large, high-risk, cross-cutting change with no realistic way to verify all 588 sites visually in this session — attempting it now would be reckless, not thorough. Recommend enforcing it going forward instead: every *new* button written from here on (Phases 1-6) should use `components/Button.tsx`, and existing screens get migrated incrementally as each phase's work naturally touches them — same incremental approach as `EmptyState`/`ErrorState` above. The two red tokens (`primary-red` vs generic Tailwind reds) should also converge as part of that same incremental pass, not a separate mass find-replace.

---

## Phase 1 — Pricing, Plans & Feature Gates ✅ Complete
(`feedback.md` §2)

Audited before building anything (same approach as Phase 0). Ground truth found:
- Plans are **not hardcoded in the frontend** — `PlanSelectionModal`/`PricingMain`/`OrderSummary` fetch live from `GET /admin/plans` and render `plan.name`/`plan.price` from real data. Retargeting pricing tiers is primarily a **backend data change** (the seeded `Plan` documents), not a frontend rewrite. A few isolated hardcoded fallbacks exist for when live data fails to load — listed in 1.5 below.
- Only 4 plans are actually seeded today: `free`/`founder`/`growth`/`enterprise` ($0/$24.99/$49.99/$149.99) — not the Go/Go Student/Pro/Business/Custom in `feedback.md`'s target pricing table.
- **Real bug confirmed**: `services/auth-service/seed-plans.js` and `services/admin-service/seed-plans.js` seed different `free`-plan limits and disagree on which plan is `isPopular` — whichever runs last against the shared DB silently wins. Two sources of truth for the same data.
- **Real bug confirmed**: Redis usage counters (`usage:{userId}:{feature}`) are never cleared when a subscription auto-renews via Stripe (`subscriptionWorker.js`'s renewal path advances `periodStart`/`periodEnd` but never touches Redis) — so usage silently carries over into the new billing period. `Subscription.increment()` (the one place that *would* reset counters) is dead code, never called anywhere.
- **Real inconsistency confirmed**: old plan-name aliases (`startup`/`basic`/`pro`/`standard`) are remapped to `founder`/`growth` in 4 different places, and two of them (`purchaseSubscription` vs `adminAssignSubscription`) map in **opposite directions** — `startup` becomes `founder` in one and `basic` in the other.
- Cancellation is a real, working flow (Stripe `cancel_at_period_end`). Downgrade is **not** a distinct flow — it's the same purchase endpoint as upgrade, which overwrites limits/plan immediately with no proration or period-end deferral; one screen (`PlansOverview.tsx`) even hides the Downgrade button entirely.
- Student ID verification: **zero existing code** anywhere — full greenfield build. Scope decision made: full workflow (upload → S3 storage with retention limit → admin approval queue → gated $10 price on approval).

### 1.1 New plan tiers (Go / Go Student / Pro / Business / Custom) `[BE]` ✅
- [x] Reconciled the two disagreeing `seed-plans.js` files: `services/auth-service/seed-plans.js` is now the single canonical source (exports `{ plans, seedPlans }`); `services/admin-service/seed-plans.js` is a 3-line delegator that requires and runs it. Both are still independently runnable (`node services/admin-service/seed-plans.js` still works), but there's only one dataset now.
- [x] Replaced the 4 old seeded plans with `feedback.md`'s target table — verified live in MongoDB: Go $15/$12 annual, Go Student $10/$8, Pro $39/$31 (isPopular), Business $99/$79, Custom $0 ("contact us", all limits `-1` unlimited via the existing sentinel), plus `free` kept as an internal-only zero-limit fallback (not marketed — see below). AI limits match `feedback.md`'s table exactly (250/40/10/1 website for Go; 1,000/200/100/3 for Pro; 4,000/800/500/10 for Business); other dimensions (social/legal/sales/team/web-builder-session limits) are this script's own reasonable scaling, documented as such in the file's header comment since `feedback.md` doesn't specify them.
- [x] Added a `file_uploads` limit field (didn't exist before) to `Plan.js`/`Subscription.js` (both `usage` and `allowedLimits`) in both auth-service and admin-service's model copies — needed for the 4th column for feedback.md's AI limits table. Enforcement wiring belongs to Phase 2 once chat file uploads are actually built as a feature.
- [x] Investigated whether `free` could just be removed (`feedback.md` groups it with Founder/Growth as labels to "remove or map") — it can't: it's a load-bearing fallback used when a user has no subscription yet or one has fully expired (`createSubscriptionWithFrozenLimits`, `subscriptionWorker.js`'s expiry path), referenced in 8+ places. Kept it, seeded with zero limits and the display name "No Active Plan" so it's honest rather than pretending to be a real product tier; not shown in the public pricing list (`PlanSelectionModal`/`PricingMain`/`PricingSection` already filter `p.key !== 'free'`).
- [x] Built one shared `normalizePlanKey`/`tierOf` utility (`services/auth-service/src/utils/planKeyMap.js`, mirrored in `services/admin-service/src/utils/planKeyMap.js` for the same cross-service-require-safety reason as `redisClient.js`) and replaced **6 separate inline copies** of the legacy-plan-name remapping logic that had drifted (found via a full-repo grep, not just the 2 originally suspected): `paymentController.js` (×2 — `purchaseSubscription` and `adminAssignSubscription`, which mapped `startup`/`pro` in *opposite* directions from each other), `subscriptionController.js`, `subscriptionHelper.js`, `userController.js` (a 5th copy the original audit missed), and `admin-service/adminController.js` (a 6th, in a different service entirely). Mapping: `founder/startup/basic → go`, `growth/standard → pro`, `enterprise → business` (justified by `feedback.md`'s own tier descriptions almost verbatim matching the old plan descriptions — Go: "Individual founder or basic user", Pro: "Growing business").
- [x] Also fixed a latent bug found while touching this code: `purchaseSubscription`'s `isUpgrade` check compared the *raw* unnormalized `plan` input against the *normalized* stored `subscription.plan`, which could misclassify a plan change in edge cases — now both sides use the normalized key.
- [x] Existing-subscriber migration: not applicable in this environment (only the seeded demo/admin accounts exist locally, no real paying subscribers to migrate) — the normalization utility itself is exactly the migration path for any subscriber still on an old key, applied automatically the next time their subscription is read/written.
- Verified end-to-end: ran the seed script against local Mongo, confirmed all 6 plans in the DB with correct prices/limits.

### 1.2 Usage counters & reset `[BE]` ✅
- [x] Fixed the confirmed bug: `subscriptionWorker.js`'s Stripe-auto-renewal branch now clears `usage:{userId}:*` Redis keys and calls `usageSyncService.markDirty` (same pattern already used by the purchase/webhook/admin-assign paths) before rescheduling the next cycle's jobs.
- [x] Removed `checkLimit`/`increment`/`canUseFeature` from `Subscription.js` — all three were already self-marked "Deprecated" in their own comments, called nowhere in the codebase (confirmed by grep), and `increment`'s reset logic used a different, incorrect field set from the real schema anyway. Genuinely dead, not a real reset mechanism to preserve.

### 1.3 Downgrade flow `[BE+FE]` ✅
- [x] `[BE]` Built a real downgrade path instead of reusing the immediate-overwrite purchase endpoint: new `Subscription.scheduledDowngrade { planKey, effectiveDate }` field (same principle as `cancelAtPeriodEnd`), new endpoints `POST /user/payment/downgrade-subscription` (validates it's actually a lower tier via `tierOf`, schedules it for `periodEnd`) and `POST /user/payment/cancel-downgrade` (revert), and application logic added to `subscriptionWorker.js`'s renewal branch — updates the Stripe subscription's price with `proration_behavior: 'none'` (correct since it fires exactly at the period boundary) and swaps in the new plan's limits, right alongside the usage-reset fix from 1.2.
- [x] `[FE]` Un-hid the Downgrade button in `PlansOverview.tsx` (was unconditionally hidden via `{buttonState.text !== 'Downgrade' && (...)}`) and in `PlanSelectionModal.tsx`'s equivalent flow; both now route downgrade clicks to a confirmation (`AlertModal`, not the payment modal — no new payment method needed) that calls the new endpoint. `CurrentPlanCard.tsx` shows a "Your plan will change to X on [date]" banner with a "Keep current plan" undo action when a downgrade is scheduled.
- Verified: syntax/type-checked; live Stripe call paths (schedule → apply-at-renewal) not yet exercised end-to-end — needs the Stripe test key (see 1.6).

### 1.4 Student ID verification — full workflow `[BE+FE]` ✅
- [x] `[BE]` `StudentVerification` model (user, `idImageKey`, status pending/approved/rejected, submitted/reviewed dates, reviewer, rejection reason, `expiresAt`). Upload via the existing multer pattern already used for profile pictures. **Deliberately did not expose a public or signed S3 URL** for something this sensitive — added `s3Service.getObjectBuffer()` (server-side `GetObjectCommand`, no new npm dependency needed) so the ID image is only ever streamed through an authenticated, admin-only route, never linkable/shareable. The image is deleted from S3 immediately on either approve or reject — never retained past the review decision.
- [x] `[BE]` `purchaseSubscription` now rejects `go_student` purchases with a clear `requiresStudentVerification` flag unless there's an approved, unexpired (`expiresAt`, 1 year) verification on file — satisfies "re-verify eligibility periodically."
- [x] `[FE]` `StudentVerificationModal.tsx` — upload UI wired into the plan-selection flow: selecting "Go Student" now checks verification status first (via a new `proceedToUpgrade` gate shared by both plan-selection entry points) and shows this upload flow instead of the payment modal when not yet approved; shows pending/rejected/expired states appropriately.
- [x] `[FE]` Admin review queue at `/admin-dashboard/student-verifications` (new sidebar link) — tabs by status, expand-to-view-image (fetched as an authenticated blob, not a plain `<img src>`, since the endpoint requires a bearer token), approve/reject with an optional rejection reason.
- **Verified fully end-to-end** (not just typechecked): uploaded a real test image as the demo user → confirmed it appears in the admin pending list with populated user info → streamed the image back byte-for-byte via the admin-only endpoint → approved it → confirmed the demo user's own status flips to `approved` with a 1-year `expiresAt` → confirmed the S3 object is actually deleted (subsequent fetch correctly 404s).

### 1.5 Frontend plan/billing UI fixes `[FE]` ✅
- [x] Reset/renewal date now uses `useUserTimezone`'s real IANA timezone via `Intl.DateTimeFormat(..., { timeZone: ianaTimezone })` in `CurrentPlanCard.tsx`, instead of plain `toLocaleDateString()` (browser-implicit timezone) — feedback.md §10.
- [x] Updated hardcoded fallback prices in `CurrentPlanCard.tsx` and `OrderSummary.tsx` (which disagreed with each other before) to the new Go/Go Student/Pro/Business numbers.
- [x] Updated the `getPlanTier` tier-ordering helper in **both** `PlanSelectionModal.tsx` and `PlansOverview.tsx` (a second copy the original audit didn't separately call out) from `free/founder/growth/enterprise` substring matching to the 5 new keys.
- [x] Updated admin-facing plan lists: `SubscriptionTab.tsx`'s dropdown now fetches live from `planService.getAllPlans()` instead of a hardcoded `["Free","Founder","Growth","Enterprise"]` array (its badge-coloring logic, which runs unconditionally regardless of the edit form being open, was also fixed — it would otherwise have shown the wrong color for every new plan name); `UserManagementTable.tsx`'s filter dropdown updated to the new tiers, and its `getSubscription` dummy-data fallback's plan names updated too (flagged, but not fully removed — that fallback firing at all is a placeholder-data concern closer to Phase 0's scope, noted inline as a TODO rather than silently left or over-scoped into a new investigation here).
- [x] Removed dead `PLAN_FEATURES` in `userDetailsConstants.ts` (confirmed zero usages).
- [x] Also fixed, found while doing this pass: `PricingMain.tsx`/`PricingSection.tsx` special-cased `plan.name === 'enterprise'` to render the "Contact for pricing" CTA — updated to check the new `custom` key instead, otherwise the Custom tier would have rendered a normal (wrong, since price is $0) "Subscribe" button instead of the contact CTA.
- [x] `BuySubscription.tsx`'s default plan fallback (`searchParams.get('plan') || 'Founder'`) updated to `'Go'`.

### 1.6 Test matrix ✅ — matching Stripe test keys provided, full matrix run live
- [x] Backend-only verifications completed first without Stripe: plan seeding, plan-key normalization end-to-end, and the full student-verification lifecycle (see 1.4) — all exercised live against the local Docker Mongo/Redis.
- [x] **Purchase**: bought Pro ($39) with a Stripe test card (`pm_card_visa`) — real Stripe subscription + invoice created, `allowedLimits` froze correctly to Pro's exact numbers.
- [x] **Upgrade**: Pro → Business — plan/limits/amount all updated correctly ($99, 5 team members).
- [x] **Downgrade**: scheduled Business → Pro — confirmed `scheduledDowngrade` set with the correct `effectiveDate`, and critically, the **plan stayed Business** (not immediately downgraded) until that date, exactly as designed.
- [x] **Cancel scheduled downgrade**: confirmed it clears cleanly.
- [x] **Cancellation**: `cancelAtPeriodEnd` correctly set to `true`, subscription stays `active` until period end (not immediately revoked).
- [x] **Reactivation**: re-purchasing after cancellation correctly clears `cancelAtPeriodEnd`.
- [x] **Failed payment**: added a Stripe decline-test card (`pm_card_chargeDeclined`) — failed cleanly with a clear error message, server stayed healthy (no crash, no silent success).
- [x] **Renewal** — this is where testing earned its keep. Simulated a renewal (advanced `periodEnd` into the past, enqueued the same BullMQ job the real scheduler uses, let the live worker process it) and initially got a **wrong result**: the subscription was fully expired to Free instead of renewing, and Redis usage wasn't cleared.

  **Root cause — a real, severe, pre-existing bug, unrelated to anything built in this phase**: `stripe` SDK `^20.1.0` moved `current_period_start`/`current_period_end` off the `Subscription` object onto each subscription *item* in a past API version bump. Every place in this codebase that read `stripeSub.current_period_end` (the exact renewal-detection check in `subscriptionWorker.js`, plus `paymentController.js`'s period-setting on purchase) was silently getting `undefined` → `Invalid Date` → a comparison that can never be true. **This meant the "did Stripe auto-renew" check could never succeed for any subscriber, ever** — every subscription would have been silently reset to Free at its very first renewal check in production, regardless of whether Stripe had actually renewed it successfully. This is exactly the kind of bug that stays invisible until the first real billing cycle completes, discovered here only because the test matrix simulated one.

  **Fixed**: new `services/auth-service/src/utils/stripeHelpers.js` (`getSubscriptionPeriod`) reads from the item level first (matching this SDK version) with a fallback to the subscription level (in case Stripe reverts or an older API version is ever used), wired into both `subscriptionWorker.js` and `paymentController.js`. Re-ran the same renewal simulation after the fix: subscription correctly detected as renewed, **Redis usage counters cleared** (1.2's fix, now actually reachable), **and the scheduled downgrade correctly applied** (1.3's fix, also now actually reachable) — all three Phase 1 backend mechanisms verified together in one real test.
- [x] Also found and fixed while testing: `addPaymentMethod` reused the raw `pmId` input for `retrieve()`/DB storage instead of the ID `stripe.paymentMethods.attach()` actually returns — harmless for real Stripe-Elements-created payment methods, but breaks with Stripe's reusable test tokens (`pm_card_visa` etc.), which is exactly what surfaced it.
- [x] Also noted, not chased further: a transaction record's `previousPlan` field appears to always equal the *new* plan rather than the actual prior one (likely captured after `subscription.plan` is already mutated in place) — a minor billing-history cosmetic/audit-trail accuracy issue, not a functional blocker, flagged for a follow-up pass rather than expanding scope here.
- [x] Cleaned up test artifacts afterward: cancelled the real (test-mode) Stripe subscription created during testing, removed the test card, restored the demo account to its intended Business-tier showcase state via `seed-demo.js` (also updated that script's hardcoded `'enterprise'` plan label to `'business'` while touching it, and re-ran it to actually apply that to the live record).

### 1.7 Found during the user's own browser spot-check `[BE+FE]` ✅
Everything in 1.1-1.6 was tested against `auth-service` directly (port 3001) or via `tsc`/`vite build` — **never through the actual gateway the real frontend uses**, and never actually looked at in a browser. The user caught two real, concrete bugs within minutes of looking:
- [x] **"Route not found" on the admin verification queue and (undiscovered until checked) the downgrade endpoints too**: `api-gateway/routes/user.js` turned out to be a hand-maintained *allowlist* of proxied routes, entirely separate from `services/auth-service/src/routes/user.js` — adding a route to the service was never enough on its own; the gateway needed its own matching entry or it 404s before ever reaching auth-service. All 6 new Phase 1 endpoints were missing from it (`downgrade-subscription`, `cancel-downgrade`, `student-verification` GET/POST, `admin/student-verifications` GET, its `/approve`/`/reject`). Added all 6. The image-viewing endpoint needed special handling — the shared `proxyRequest` helper always wraps upstream responses as JSON, which would have corrupted the raw image bytes, so it gets a dedicated binary passthrough instead. Verified all of them through the real gateway afterward (not directly against auth-service this time).
- [x] **"No Active Plan" ($0, `free`'s display name) appeared as a selectable/downgradable tier in the Settings page's plan grid** (`PlansOverview.tsx`) — the free-tier filter from 1.5 was applied to `PlanSelectionModal`/`PricingMain`/`PricingSection` but this 4th place fetching the same plan list was missed. Fixed with the same filter.
- [x] Also cleaned up a loose end while fixing this: the `seed-demo.js` plan-label edit from 1.6 had been made but never re-run against the actual demo record, so it was still raw-labeled `enterprise` (functioned correctly either way via `normalizePlanKey`, but wasn't clean) — re-ran it.

**Takeaway**: the gap between "typechecks and builds" and "actually works when a user clicks it" was real, not hypothetical — both bugs here were fully invisible to `tsc`/`vite build`/direct-service curl tests, and both would have shipped broken to real users going through the real app.

**Follow-up self-check**: after fixing the gateway allowlist, only re-tested 2 of the 8 affected routes through it before declaring it fixed — the same "assumed rather than verified" mistake that caused the bug in the first place. Went back and tested all 8 through the actual gateway: file upload (the highest-risk one, passing multipart through two proxy layers), the custom binary image-passthrough, approve, reject, and — critically — `downgrade-subscription` itself, not just `cancel-downgrade`. That one initially 404'd with "Active subscription not found," which turned out to be *correct* behavior (the demo account had no Stripe-linked subscription at that moment, a side effect of cleanup, not a bug) — confirmed by setting up a real Stripe-linked purchase and re-testing, which succeeded. All 8 now confirmed working through the real path. Cleaned up the resulting test Stripe subscription/card and restored the demo account afterward.

---

## Phase 2 — Ninja Chat `[BE+FE]` ✅ complete, verified live in browser
(`feedback.md` §3)

Audited before building anything (same approach as Phases 0-1). Ground truth found:
- LLM is **OpenAI** (a fine-tuned model, `openaiService.js`), not Gemini — `GEMINI_API_KEY`/Gemini CSP entries are vestigial dead config despite the naming suggesting otherwise; a stale code comment even says "Generate response from Gemini" while calling OpenAI.
- **No real streaming exists at all.** The "typing" effect (`typeWriterAppend`, `AIChat.tsx`) is a pure client-side animation over a response that already arrived in full — `requestAnimationFrame` slicing an already-complete string at a fixed chars/sec rate, not incremental network delivery. Chose not to rearchitect this into real SSE streaming for this phase (see 2.4) — it would also require special gateway proxy handling, same class of problem as the binary-image-passthrough fix in Phase 1, for a UX improvement `feedback.md` doesn't explicitly demand.
- Nothing exists yet for web search, file upload/parsing, chat history search (no text index), Edit Prompt, Regenerate, or memory — all genuinely greenfield, confirmed via full-repo grep before assuming so.
- `Copy Response` and chat rename already work. Markdown/code-block rendering already works well (`react-markdown`+`remark-gfm`+`rehype-highlight`); GFM tables render but with no custom styling.
- The quota-enforcement pattern from Phase 1 (`checkLimit(token, feature, increment)`) is clean and directly reusable for `file_uploads`, which already exists as a Plan/Subscription limit field (added in Phase 1, unused until now).
- Two external decisions made with the user: **Tavily** for web search (needs an API key), and memory as **manual save** (not automatic extraction) for now — matches `feedback.md`'s literal ask ("storage + a page to view/edit/delete saved memories") without the added complexity/cost of an extra LLM call per turn to auto-detect memorable facts.

### 2.1 Web search + citations `[BE+FE]` ✅ backend done+verified
- [x] `[BE]` `services/ai-chat/src/utils/tavilyService.js` — calls Tavily when the user explicitly enables search or the message looks like it needs current info (`looksLikeSearchQuery` heuristic: "latest", "current", "news", "price of", etc. — avoids the extra API call/latency on every ordinary message), returns `sources: [{title, url, snippet}]`, fails soft (empty array) on any error so search never breaks a chat send.
- [x] `[BE]` `sources` added to the `AiChat` message schema and the `POST /chat` / regenerate / edit response shapes.
- [x] **Verified live, not just typechecked**: asked "What is the latest news about SpaceX today?" — got back real Tavily results (cbsnews.com, bloomberg.com, spacex.com, etc. with actual snippets) and an assistant answer that correctly synthesized current information outside the model's training data.
- [x] `[FE]` Citations list under assistant messages (`ChatMessage.tsx`) — clickable chips with an external-link icon, snippet in the `title` tooltip, `target="_blank"`. Manual search toggle button added in the composer (red pill, `aria-pressed`) alongside the existing backend heuristic.
- [x] **Verified live in a real browser** (Playwright, not just typecheck): toggled Search, asked the SpaceX question, got 5 rendered citation chips matching the real Tavily results.

### 2.2 File upload pipeline + document Q&A `[BE+FE]` ✅ backend done+verified
- [x] `[BE]` Multer upload on `POST /chat` (up to 5 files, 20MB each), type validation (PDF/DOCX/XLSX/CSV/TXT/JPG/PNG — PPTX accepted but not text-extracted, no parser library used for it), gated by `checkLimit(token, 'file_uploads', true, fileCount)` — same reusable pattern as `ai_chat_messages`.
- [x] `[BE]` `services/ai-chat/src/utils/documentParser.js` (new deps: `pdf-parse`, `mammoth`, `xlsx`) + `services/ai-chat/src/utils/s3Service.js` (new, ai-chat didn't have one — mirrors the auth-service/Phase-1 pattern) for storage.
- [x] `[BE]` Extracted text folded into the OpenAI system prompt as attachment context.
- [x] **Verified live end-to-end**: uploaded a real .txt file with fabricated facts (budget, name), asked a question referencing it — got the exact correct answer back, proving real extraction+context-injection, not a stub. Separately confirmed the file actually persisted in S3 (fetched it back byte-for-byte).
- [x] **Caught and fixed a real bug during testing**: `seed-demo.js`'s hardcoded demo-account limits predated the Phase-1 `file_uploads` field and left it at the schema default of 0 — uploads 403'd until fixed.
- [x] `[FE]` Attachment button + drag-and-drop zone in `AIChatComposer.tsx` (client-side type/size/count validation mirroring the backend's `SUPPORTED_TYPES`), file chips with remove, attachment chips rendered on sent user messages in `ChatMessage.tsx`.
- [x] `[FE]` Real (not simulated) upload progress bar + "Processing document(s)…" status, wired through axios's actual `onUploadProgress` event (`apiClient.post` already threads a full `AxiosRequestConfig`) rather than a fake timer — added after re-reading `feedback.md`'s literal "Show upload progress, document-processing status" line and confirming it wasn't covered by the generic busy spinner alone.
- [x] **Verified live in a real browser**: attached a real `.txt` file with a fabricated fact via the file input, sent a question referencing it, got back the correct answer extracted from the file — proving the upload → S3 → parse → context-injection path works end-to-end through the actual UI, not just via curl. Separately verified the progress bar itself with a larger (~2.5MB) file: "Uploading files…" then "Processing document(s)…" both rendered and cleared correctly once the response settled, with zero console errors.
- [x] **Gateway routing** — `api-gateway/routes/ai-content.js` turned out to have the *same* problem as Phase 1's `user.js`: its own bespoke per-route axios proxy, not the shared `utils/proxy.js` helper, and it silently dropped `GET /chats`' `?q=` search param and had no multipart path at all. Refactored the whole file to use the shared `proxyAuthenticatedRequest` helper (same one already proven correct for JSON, query strings, and multipart in Phase 1) instead of hand-rolling it again; export stays a dedicated binary passthrough (same reasoning as Phase 1's verification-image endpoint). All 8 new/changed endpoints verified through the actual gateway, not direct-service calls, before calling this done.
- [x] **Real bug found on a fresh re-audit against `feedback.md`'s literal text after declaring the phase done** — "delete stored/chat files afterward" wasn't actually true: `deleteChat` only removed the Mongo document, leaving uploaded files orphaned in S3 forever (a real data-hygiene issue for anything sensitive). Same class of bug existed in `editMessage`: truncating messages after an edit discarded their attachments' S3 keys without cleanup. Fixed both (`chatController.js`) and **verified live**: uploaded a file, confirmed it existed in S3, deleted the chat (or triggered the truncation via edit), confirmed the object was actually gone (`NoSuchKey`) — not just that the DB record disappeared.
- [x] **Scope decision made with the user** (matches the Tavily/memory-scope pattern earlier in this phase): max file size stays a flat 20MB cap for everyone rather than plan-tiered — upload *count* is already plan-gated (`file_uploads` limit), size tiering deferred until it's a real product need. Not a gap; a deliberate, disclosed call.
- [x] **Scope decision made with the user**: no per-attachment delete from an existing chat — whole-chat delete (which now correctly cascades to S3) covers the "delete stored/chat files" ask; per-file deletion mid-conversation deferred as unneeded for now.

### 2.3 Chat history search `[BE+FE]` ✅ backend done+verified
- [x] `[BE]` MongoDB text index on `AiChat` (title weighted 5x over `messages.content`) + `?q=` param on `GET /chats`, using `$text`/`$meta: "textScore"` sort.
- [x] **Verified live**: searched for text actually present in a prior message ("lead engineer") → correct chat returned; searched for a nonexistent term → empty array, not an error; also confirmed (not a bug) that attachment text isn't included in the index, matching `feedback.md`'s literal scope (titles/prompts/responses).
- [x] `[FE]` Fixed search bar in `ChatHistorySidebar.tsx`, debounced (300ms), matching-snippet built client-side from the already-returned `chat.messages` (no extra backend call), date + title per result, clicking a result opens that chat directly.
- [x] **Verified live**: searched "BANANA" (text present in several prior messages, not titles) — sidebar correctly returned only the matching chats with the right snippet excerpt and date, not the whole unfiltered list.

### 2.4 Chat actions `[BE+FE]` ✅ backend done+verified
- [x] New Chat / Copy Response — already worked, confirmed unaffected.
- [x] `[BE]` Edit Prompt (`PUT /chat/:chatId/message/:messageId`) — truncates everything from the edited message onward and regenerates. **Verified live**: edited "say BANANA" → "say APPLE" mid-conversation, got back "APPLE", re-fetched the chat and confirmed exactly 2 messages remained (old turn cleanly discarded, not left dangling).
- [x] `[BE]` Regenerate (`POST /chat/:chatId/regenerate`) — drops the last assistant message, re-runs generation for the same last user message. **Verified live.**
- [x] Extracted a shared `runAssistantTurn()` helper (search+memory+generation+usage-reporting+follow-ups) used by send/edit/regenerate identically, instead of tripling that logic — found while implementing, not a pre-existing pattern.
- [x] `[FE]` Edit Prompt UI (pencil icon on user messages, prefills the composer, "editing" banner with cancel), Regenerate button (last assistant message only), Stop Generation (cancels the client-side reveal and jumps to full text if the response already arrived, or unblocks the composer immediately if still awaiting the network — matches the phase-level scoping note; no real request cancellation).
- [x] **Backend gap found and fixed while wiring this up**: `generateChatMessage`/`editMessage`/`regenerateMessage` never returned each message's `_id`, so the just-sent message in the current session had no id to edit — the Edit button silently never rendered until a full reload. Added `_id` to all three response shapes (`chatController.js`), verified via curl before touching the frontend.
- [x] **Frontend bug found and fixed via live browser testing, not typecheck**: the optimistic local user-message object was never replaced with the server's version after send, so it still lacked `_id` client-side even after the backend fix — `AIChat.tsx`'s `submitUserMessage` now swaps in the server-confirmed message on that code path.
- [x] **Second bug found and fixed via live browser testing**: clicking Stop Generation correctly reset the parent's `isGenerating`/`isRevealing`, but `AIChatComposer.tsx` has its own internal `isSubmitting` flag that only clears once the original (still in-flight, uncancellable) network promise resolves — the composer stayed disabled until the real response eventually landed. Fixed by having the Stop click handler reset the composer's own `isSubmitting` immediately (`handleStopClick`).
- [x] **Verified live in a real browser** (Playwright driving the actual login → chat flow, not curl): plain send shows Copy/Edit/Regenerate/Remember buttons + 3 follow-up chips; edit truncates and regenerates correctly with an "edited" badge; regenerate swaps the response; Stop Generation re-enables the composer immediately.
- [x] **Third bug found on a second re-audit pass (after already declaring the phase done twice)**: entering Edit mode on an earlier message, then instead clicking a follow-up chip or Regenerate, left `editingMessage` state and the "Editing message" banner/prefilled draft dangling — the next normal send would then incorrectly re-truncate the conversation from the stale edit target. Also, attaching a file while mid-edit was silently dropped (`editMessage` has no file support server-side) with no error and a leftover file chip. Fixed by clearing `editingMessage`/`selectedFiles` whenever a follow-up or regenerate fires, and hiding the attach button entirely while editing (`AIChat.tsx`). **Verified live**: entered edit mode (banner shown, attach button gone), clicked a follow-up chip, confirmed the banner cleared and the attach button reappeared, then sent a normal new message and confirmed it landed as a fresh message rather than hijacking the stale edit.
- [x] **Fourth bug found on the same re-audit pass**: Copy Response and Remember This were both clickable while the last assistant bubble was still mid-typewriter-reveal, so clicking either during that window would copy/save the truncated partial text instead of the final answer — a real correctness bug (Copy Response predates this phase; Remember This is new in it, and inherited the same flaw). Fixed by hiding both for the specific message that's actively revealing (`isRevealingThisMessage` in `ChatMessage.tsx`, scoped to the last assistant message during `isGenerating`, not a blanket hide across the whole chat). **Verified live**: sampled the DOM mid-reveal (Stop-generating visible + partial text present) and confirmed both buttons were absent (count=0), then confirmed both reappeared once the response settled (count=1).
- [x] **Fifth bug found probing error handling directly (not just the happy path)**: `editMessage`/`regenerateMessage` had no `:chatId`/`:messageId` format validation, so a malformed ID leaked a raw Mongoose `CastError` ("Cast to ObjectId failed for value...") as a 500 — worse than `feedback.md`'s "useful errors" ask, and inconsistent with `deleteChat`/`getChatHistory`'s existing clean-error convention. Added `param("chatId").isMongoId()` / `param("messageId").isMongoId()` to both routes (`chat.js`) plus a missing `validationResult` check in `regenerateMessage` (it had none at all). **Verified live**: malformed IDs now return a clean `400 {"message":"Validation failed","errors":[...]}` instead of a leaked 500; re-confirmed valid regenerate/edit calls still work correctly afterward (no regression).
- **Also investigated, ruled out as a Phase 2 issue**: intermittent `503`/generic `"Error"` responses seen sporadically throughout this session's curl-based verification (always transient, always succeeded on immediate retry). Confirmed this is unrelated to the deterministic CastError bug above (retrying that one reproduces the exact same error every time). Never once reproduced during any of the ~4 full Playwright browser runs across this phase — only under rapid back-to-back curl bursts, a load pattern no real user produces via the UI. Treated as a local dev-environment/testing-harness artifact, not a product bug; not chased further.
- [x] **Sixth bug found checking an angle none of the prior ~5 browser runs had covered: mobile viewport.** Every prior live verification used a fixed 1400×900 desktop viewport; at a real mobile width (390×844) the app-wide mobile nav hamburger (`DashboardSidebar.tsx`, `z-[60]`) visually overlapped the "Chat History" title and its own dimming backdrop, because `ChatHistorySidebar.tsx`'s mobile overlay only used `z-50`/`z-10`. Confirmed this z-index value predates Phase 2 entirely (present in the very first read of the file, before any edits) — a pre-existing cross-cutting bug, not a Phase 2 regression — but it sits directly in the header region Phase 2 just added the search bar to, and the fix is a trivial, zero-risk, single-component change, so fixed it now rather than deferring: bumped the sidebar to `z-[70]` and its backdrop to `z-[65]` (both above the hamburger's `z-[60]`, mobile-only — `lg:` values untouched). **Verified live**: screenshot before showed the hamburger icon overlapping the title text; after, the header renders cleanly with no overlap, composer/buttons/follow-up chips all fit within the 390px viewport with no horizontal overflow.

### 2.5 Suggested follow-up prompts `[BE+FE]` ✅ backend done+verified
- [x] `[BE]` `generateFollowUps()` in `openaiService.js` — a short follow-up-only completion call after the main response, returns exactly 3 short questions. Wired into send/edit/regenerate via the shared helper.
- [x] **Verified live**: every test message above returned 3 relevant, on-topic follow-ups (e.g. asking about SpaceX news produced "What are SpaceX's upcoming launches?" etc.), not placeholder text.
- [x] `[FE]` Rendered as clickable pill chips below the response (`ChatMessagesList.tsx`), only shown once generation/reveal has fully settled; clicking one auto-sends it as the next message (`handleFollowUpClick` in `AIChat.tsx`, parameterized off the shared `submitUserMessage` so it doesn't race the `prompt` state's stale-closure).
- [x] **Verified live**: clicking a follow-up chip correctly sent it and produced a new on-topic response.

### 2.6 Markdown/table/code rendering pass `[FE]` ✅ done
- [x] Custom-styled table renderer added to `ChatMessage.tsx`'s `ReactMarkdown` components map (`table`/`thead`/`tbody`/`tr`/`th`/`td`) — GFM tables previously fell through to unstyled default HTML; code blocks/inline code were already styled.

### 2.7 Memory feature — manual save `[BE+FE]` ✅ done+verified
- [x] `[BE]` `Memory` model + full CRUD (`services/ai-chat/src/controllers/memoryController.js`), capped at 200 entries/user with a clear error past that rather than growing unbounded.
- [x] `[BE]` `getMemoryFactsForUser()` feeds active memories into the OpenAI system prompt via the same shared `runAssistantTurn()` helper.
- [x] **Verified live, functionally not just structurally**: saved the memory "My company is called Ninja Test Corp", then in a *separate* new chat asked "What is the name of my company?" — got back "Ninja Test Corp" correctly, proving the assistant is actually reading and using saved memories, not just storing inert data. Also verified update and delete both work correctly through the gateway.
- [x] `[FE]` "Remember this" bookmark action on every assistant message (calls `memoryService.createMemory`, toasts confirmation) + a dedicated memory management page (`src/pages/User/ChatMemories.tsx`, route `/ai-tools/chat/memories`, linked from the chat history sidebar header) with add/edit/delete, source + date metadata.
- [x] **Verified live in a real browser**: clicked "Remember this" on a response, saw the confirmation toast, navigated to the memory page, and confirmed the exact saved fact appeared there with working edit/delete controls.

### 2.8 Test matrix
- [x] **Backend**: every feature above (web search, file upload+Q&A, chat search, edit, regenerate, follow-ups, memory create/list/update/delete/use) verified live through the actual gateway (port 5000) with real data, not direct-service calls or typechecking-only — per Phase 1's lesson about the gap between "looks right" and "works through the real path."
- [x] **Frontend**: full `npx tsc --noEmit` + `npm run build` clean across the whole project (not just touched files). Then a real browser pass (Playwright driving actual Chromium against the running dev server, not curl/typecheck) covering the full golden path: login → send message → actions render → follow-up click → edit message → regenerate → remember-this → memory page → file upload+Q&A → web search+citations → chat history search → Stop Generation. All 11 steps pass, zero console errors, one benign `net::ERR_ABORTED` on a Stripe.js iframe caused by mid-script page navigation (not a Phase 2 regression).
- [x] **3 real bugs found and fixed via this live pass** (typecheck/build alone missed all three, matching Phase 1's lesson): (1) gateway route file for `ai-content` used a bespoke proxy instead of the shared helper — caught proactively before user-facing testing; (2) `_id` missing from send/edit/regenerate response messages, breaking Edit Prompt in the current session; (3) Stop Generation not fully re-enabling the composer because of a second, component-local `isSubmitting` flag independent of the parent's generating state.

---

## Phase 3 — Website Builder rebuild `[BE+FE]` ✅ complete, verified live in browser (`feedback.md` §4 — "the largest single UI rework in the list")

Audited before building anything (two parallel deep-dive agents, frontend + backend), same discipline as Phases 0-2. Ground truth found — this phase is materially bigger than the checklist implies:

- **The editor is not custom-built.** `WebsiteBuilderStudio.tsx` is a thin wrapper around the commercial `@grapesjs/studio-sdk` — most "editor chrome" (Pages, Layers, Asset Manager, Global Styles, undo/redo, device switcher) is the SDK's own stock UI, reskinned via a `layout` config, not hand-rolled React. This shapes everything below: "Content/Design/Pages/Settings" and "Ask Ninja" don't exist as SDK primitives and have to be built as custom panels layered on top of the SDK's own layout system — precedented (the current code already does this for 8 other panels) but real, non-trivial work per panel.
- **Zero backend scaffolding for templates.** No `Template` model, no seed data, no template endpoints — the 9 existing templates are raw HTML strings hardcoded in one 3655-line frontend file (`config/DemoTemplates.tsx`), no `industry`/`category` field on any of them. The spec's 20-30-templates-across-16-industries ask starts from a blank slate on the backend and needs ~11-21 new templates designed, not just categorized.
- **Zero AI integration in `website-builder-service`.** No OpenAI/LLM calls anywhere, no AI endpoints. Ask Ninja, AI Rewrite/Redesign Section, and AI Regenerate Image are all fully greenfield — this is effectively repeating Phase 2's "add an LLM integration to a service that has none" work, but for a different service.
- **`websiteData`/`pageData` are opaque Mongoose `Mixed` blobs**, backend-unstructured. Section add/duplicate/delete/reorder today only happens via generic GrapesJS canvas/layer manipulation, persisted wholesale on every save — there's no backend-enforced "sections" concept to hang AI Rewrite/Redesign Section off of without the frontend telling the backend which chunk of the blob it means.
- **No version history capacity at all** — one live `websiteData` per page, overwritten on every autosave. Auto-save itself does exist (SDK storage manager, ~100s/100-change interval) and undo/redo exists (SDK stock, in-session only), but there's no persisted "saved" indicator and no revision storage to restore from.
- **Real, already-broken things found, not just gaps**: (1) `WebBuilder.tsx`'s "Recent Websites" fetch has a genuine auth-hydration race condition (empty-deps `useEffect` fires before `AuthProvider` finishes hydrating `user` from storage) that can silently skip the fetch and show a false "No Websites Yet" — very likely the literal "fix Recent Websites loading" complaint in `feedback.md`. (2) Two independent, out-of-sync SEO surfaces — the dashboard's `SEOSettingsModal` writes to `seoSettings`, but the in-editor SDK settings panel writes to a different field inside `websiteData`, and published-HTML generation appears to only consume the latter — meaning the dashboard SEO modal may not affect the live site's meta tags at all. (3) Gateway bugs matching the established pattern: `GET /analytics/:websiteId` has no gateway route (404s), and `GET /gallery/:filename` vs the gateway's `GET /gallery/:userId/:filename` path-segment mismatch breaks gallery asset serving through the gateway. (4) No delete-website endpoint exists anywhere (backend or gateway). (5) `SYSTEM_IP` for custom-domain A-record verification defaults to `127.0.0.1` with a `// Replace with real IP` comment — non-functional stub. (6) Two duplicate/dead code paths: `AssetService.ts` (unused, superseded by `GalleryService.ts`) and `PagesManagerModal.tsx` (fully built, fully unwired — commented out of the dashboard).
- Confirmed NOT duplicate entry points as the spec implies — it's one creation modal, just inconsistently labeled ("Start New Website" button → modal titled "Create a Project", form fields named `projectName`/`projectDescription` throughout). The fix is a rename/copy pass, not a dedup of two flows.

Given the size, breaking this into sub-phases (mirroring Phase 1/2's structure) rather than one monolithic pass:

### 3.1 Creation flow — 3-step wizard `[FE]`
Two scope decisions made with the user before starting (matches the Tavily/memory-scope pattern from Phase 2 — both material enough to change the shape of the work):
- **Draft generation**: Step 3's draft is a **smart template-fill**, not AI-generated — pick the best-matching static template for the chosen vibe/industry and substitute in the business name/description, no AI call. "Tell Ninja What to Change" ships as a stub now, wired to real AI section editing once 3.4 exists.
- **Template count**: build the wizard against the **existing 9 templates** (mapped to best-fit vibes/industries) now; expanding to 20-30 across all 16 industries is a separate, later content-authoring task (3.2), not a blocker for the wizard itself.

- [x] Replaced `CreateWebsiteModal.tsx` (deleted, fully superseded) with `CreateWebsiteWizard.tsx` — a real 3-step wizard: (1) business name + description required, industry/goal optional (dropdowns; logo/brand-colors intentionally **not** built — no backend field exists to persist either one, see note below), info draft-persisted via `useDraftPersistence` so reopening the wizard doesn't lose input; (2) 7 vibe cards (Modern/Minimal/Bold/Elegant/Local Business/Portfolio/Let Ninja Choose), each mapped to a best-fit template from the existing 9 via `config/vibeTemplates.ts` (`pickTemplateForVibe`), "Let Ninja Choose" uses a keyword heuristic against the optional industry field; (3) review screen showing the template with hero heading/subheading swapped for the real business name/description (`fillTemplateDraft` — DOM-scoped to the home page's first `<h1>`/nearby `<p>` only, so it can't clobber other pages or long body copy), with Keep This / Change Style / Tell Ninja What to Change (stub toast) / Preview (full-screen modal) / Edit / Publish.
- [x] **Logo upload + brand color — built and verified live.** Originally cut (no backend field existed to persist either one to, so the inputs would have been fake controls); closed out once that was fixed. Added `logoUrl`/`brandColor` fields to `WebsiteBuilder` (accepted directly in `create()`'s body), a Step 1 file-upload control (reusing the existing gallery-upload endpoint to get a real S3 URL — no new upload pipeline needed) and a native `<input type="color">` picker. **Logo is actually applied**, not just stored: `fillTemplateDraft` inserts an `<img>` as the first child of whatever element is `nav`'s (or `header`'s) first child — confirmed across the 20 templates' real markup that this is consistently the brand/logo area — additively (nothing removed/replaced), so it can't break a template's layout the way overwriting text could. **Brand color is deliberately persisted but not auto-injected into the template**: with 20 independently-authored templates all using inline styles (no shared CSS variables), there's no safe general way to retarget "the accent color" without per-template mapping work — a real, disclosed engineering constraint, not an oversight. **Verified live in a real browser**: uploaded a real logo file through the wizard, confirmed the "Logo uploaded" indicator, confirmed the actual `<img src="https://…s3…">` tag (real S3 URL, correct alt text, correct inline sizing) present in the Step 3 draft preview's HTML, confirmed both `logoUrl` and `brandColor` (set via a real color-picker interaction, not just a default) persisted correctly on the created website record.
- [x] Standardized on "website" language throughout this flow (business name/description, "Build Your Website" wizard title) — the old modal's `projectName`/`projectDescription`/"Create a Project" labeling is gone from this surface (not a repo-wide project/website rename, scoped to this flow as decided).
- [x] Fixed the auth-hydration race feeding false "No Websites Yet" (`WebBuilder.tsx`'s `useEffect` now waits on `user?.id` in its dependency array instead of firing once with `[]`) and the unguarded `user.id` crash-and-bounce in the editor (`WebsiteBuilderStudio.tsx`, now `user?.id`-guarded) as part of touching this flow.
- [x] **Editor hand-off**: rather than fabricate a `websiteData` shape and risk it not matching what the GrapesJS Studio SDK actually expects, the filled draft is staged in `sessionStorage` and consumed by the editor's existing `storage.onLoad` (same `{pages:[{name,component}]}` shape it already uses for blank projects and stock templates — confirmed by reading that code, not guessed). "Publish" additionally passes `&autopublish=1`, which the editor's `onReady` uses to auto-open the *existing* publish-confirmation modal (not skip straight to actually publishing without the user seeing it).
- [x] **Real bug found and fixed via live browser testing, not typecheck**: `fillTemplateDraft`'s DOM-based substitution used `DOMParser.parseFromString()` then returned only `doc.body.innerHTML` — but every template starts with a leading `<style>` tag, which the browser's HTML parser hoists into `doc.head`, not `doc.body` (confirmed empirically, not just theorized). The Step 3 preview iframe was silently rendering completely unstyled (default blue links, no layout, overlapping text) while the actual GrapesJS editor happened to render correctly (its own HTML importer is more robust to this pattern) — a real, user-visible bug that would have shipped if only the editor screenshot had been checked. Fixed by concatenating `doc.head.innerHTML + doc.body.innerHTML`. **Verified live**: before/after screenshots of the same CityCafe draft — unstyled/overlapping text before, fully styled hero with correct dark cafe theme and personalized heading after.
- [x] **Verified live in a real browser** (Playwright: login → dashboard → open wizard → fill Step 1 → pick a vibe → confirm personalized draft renders correctly in both the compact and full preview → Change Style round-trip preserves Step 1 data → Tell Ninja stub toast fires → Keep This creates the website and opens the real editor in a new tab with the personalized draft actually loaded, confirmed via screenshot, not just an element-exists check → wizard closes and dashboard list refreshes). 8/8 steps pass, zero console errors.
- Known limitation, not fixed here (tracked in 3.6): no delete-website endpoint exists anywhere in the backend yet, so the 2 websites created while testing this flow couldn't be cleaned out of the demo account afterward.

### 3.2 Template expansion `[BE+FE]` ✅ done+verified
- [x] `[BE]` New `Template` model (`services/website-builder-service/src/models/Template.js`) — `templateId` (unique slug), `name`, `industry`, `vibes: string[]`, `thumbnail`, `isActive`, `pages: [{name, component}]`.
- [x] `[BE]` Migrated the 9 existing templates out of the frontend's static `DemoTemplates.tsx` (267KB, no backend model existed at all before) via `esbuild` (transpile the `.tsx` to CJS, `require()` it, dump the array to JSON) into `seed-data/existing-templates.json`, then classified each with an `industry`/`vibes` tag it never carried before (based on actual content, e.g. `city-cafe`/`restaurant-premium` → `restaurant-coffee`, `luxury-fashion` → `e-commerce`).
- [x] `[BE]` Authored **11 new templates** covering every one of the 16 industries feedback.md names that weren't already covered (agency, real estate, personal brand, consulting, construction, law, gym\*, beauty, automotive, hotel, startup, local business) — parallelized across 4 subagents, each independently studying the existing templates' structure/quality bar first. Result: **20 total templates**, all 16 named industries covered (plus 3 bonus categories — healthcare, education, travel — carried over from the original 9). This lands at the top of the "20-30" target range agreed with the user (who explicitly scoped this to "~8-12 new templates, ~17-21 total" given the content-authoring cost) — 11 new landed better than expected because 4 agents worked in parallel.
  - \*gym was already covered by the existing `fitness-center` template.
  - Each new template: single self-contained HTML fragment (one `<style>` block + body-level markup, no `<html>/<head>/<body>` wrapper — matching the existing convention exactly, verified necessary since `fillTemplateDraft`'s DOMParser hoists a leading `<style>` into `doc.head`), real industry-specific copy (no Lorem Ipsum), real Unsplash photo URLs (HTTP-verified by two of the four agents, visually spot-checked by a third), no `<script>` tags (an explicit constraint) — the reference templates' JS-based hamburger nav was independently reimplemented as a pure-CSS checkbox/label toggle by all 4 agents without being told the specific technique, converging on the same solution.
  - Verified before seeding: all 11 files valid JSON, no `templateId` collisions (with the existing 9 or each other), no forbidden tags, all required fields present, all `vibes` values from the real allowed set.
- [x] `[BE]` `seed-templates.js` — loads `existing-templates.json` + every `seed-data/new-*.json`, seeds the `Template` collection. Ran successfully: 20 templates, all 16 target industries present.
- [x] `[BE]` `GET /website-builder/templates` (full content, optional `?industry=`/`?vibe=` filters) and `GET /website-builder/templates/:templateId` — `TemplateController.js` + gateway routes.
- [x] `[FE]` New `TemplateService.ts`; `vibeTemplates.ts` rewritten from "import a static file, match off hardcoded id lists" to pure functions taking a fetched `templates` array plus the DB's own `industry`/`vibes` fields — a template no longer needs a hardcoded map entry to be selectable by vibe or by "let Ninja choose" industry matching, it just needs the right tags on its own record.
- [x] `[FE]` `CreateWebsiteWizard.tsx` fetches the full template list once when it opens (loading state covers the edge case of reaching Step 3 before the fetch resolves); `WebsiteBuilderStudio.tsx`'s `storage.onLoad` (`?template=` stock-template lookup) and its `templates.onLoad` (the SDK's own in-editor Template picker panel) both now fetch from the API instead of a static import. `DemoTemplates.tsx` deleted — nothing imports it anymore (confirmed via repo-wide grep before deleting).
- [x] **Real bug found and fixed — the gateway was giving false-negative failures under sustained request volume, unrelated to templates specifically but caught while load-testing this feature.** `api-gateway/utils/proxy.js` created a brand-new `http.Agent`/`https.Agent` (with `keepAlive:true`) on **every single proxied request** instead of reusing shared ones — with `keepAlive` that meant each request opened its own never-reused keep-alive socket nothing ever explicitly closed, defeating the point of keepAlive and leaking sockets/handles in the long-running gateway process specifically. Confirmed live: after enough requests during this phase's testing, the gateway started getting `ECONNREFUSED` connecting to a target service that a fresh `curl` or a brand-new Node process could reach instantly — the target was fine, the gateway process's own resources were exhausted. Fixed by moving both agents to module scope (created once, shared for the process's lifetime). Verified: 15/15 rapid-fire requests to the templates endpoint succeeded after the fix, where before the same pattern reliably failed with generic `{"success":false,"message":"Error"}` 503s.
- [x] **Real bug found and fixed — `Plan.js` was duplicated in `admin-service` with its own separate Mongoose schema**, missing the `ai_website_generation` key added in 3.4 (and not touched by that phase's fix, since 3.4 only updated `auth-service`'s copy). Since Mongoose schemas are `strict` by default, any admin-panel flow that re-saves a Plan document through `admin-service`'s model would have silently dropped that field. Added the same key there too, matching `auth-service`'s `Plan.js` field-for-field.
- [x] **Verified live end-to-end through the real gateway path** (Playwright, not just curl): opened the wizard, picked "Real Estate" industry + a specific vibe → template loaded from the API and the draft preview rendered with the filled business name (existing-template path, before new content was seeded); after seeding, re-verified with two of the newly-authored templates specifically (Law → Industry="Law" + "Let Ninja choose for me" correctly selected the new `sterling-hale-law` template, not a stale/wrong one; Construction → correctly selected `ironclad-construction`) — both with the personalized business name confirmed present in the actual rendered draft HTML, both opened cleanly in the real editor with zero console errors. **Also caught and fixed a false alarm in my own test**: an early test run appeared to show "Let Ninja choose" ignoring the selected industry entirely (picked a consulting template for a Law-industry site) — traced with a temporary debug log to the actual runtime values, which proved the matching function was receiving `vibe: "minimal"`, not `"let-ninja-choose"` at all. The test script's own Playwright selector (`hasText` matching against a `div`, not scoped to `button`) had matched a large wrapping container instead of the specific vibe card and clicked its geometric center, landing on the wrong card. Not an app bug — fixed the test, re-verified, confirmed correct.

### 3.3 Editor restructure `[FE]` ✅ done+verified (scoped down from a full rewrite — see below)
- **Scope decision made unilaterally but disclosed here**: a full rebuild of the sidebar into genuine persistent Content/Design/Pages/Settings tabs (replacing the icon-rail's click-to-open-overlay interaction model entirely) was considered and rejected as too risky given what this phase already learned about the GrapesJS Studio SDK — its toolbar buttons have no accessible `title`/`aria-label` anywhere in the DOM (confirmed repeatedly in 3.5/3.7/3.9's testing), meaning a broken interaction in a full rewrite would likely be hard to detect even via careful testing, and a broken *editor* would be a far worse outcome than an imperfect information architecture. Instead: kept every existing panel and its proven `onClick` logic completely untouched, and made two purely additive changes.
- [x] Added small text captions ("Content", "Pages", "Design", "Settings") directly beneath the 4 sidebar icons that map one-to-one to `feedback.md`'s literal ask (Add Blocks→Content, Pages→Pages, Global Styles→Design, Global Settings→Settings) — a real fix for the same "icon-only, no accessible label" usability gap this session's own testing kept running into, which means a real first-time user would hit the identical problem. Secondary/advanced tools (Layers, Assets, Templates, Home) stay as plain icons, deliberately not part of the "main 4."
- [x] Built a genuine "Advanced" panel (new sidebar icon) — directly answers `feedback.md`'s "hide CSS/HTML... inside Advanced," which existed only as a fully-disabled dead end before (`display:none` + its trigger commented out, see the phase-level audit note). **Real mechanism found, not guessed**: there's no command string that opens the SDK's code view — the file's own commented-out prior attempt at this exact button revealed the working approach is a direct DOM click on the SDK's still-rendered-but-hidden stock button (`.importcode-btn button`), which the new Advanced panel now reuses instead of inventing an untested new call. (A first draft guessed a plausible-sounding command name, `core:open-code`, before this was found — never shipped, caught while re-reading the surrounding code for the real mechanism.)
- [x] Deliberately did *not* duplicate the existing "Animate Selected" toolbar button inside Advanced — it already exists and works where it is; a first draft added a second copy of it in the new panel calling a guessed, likely-nonexistent command, cut before verification once it was clear it added no real value over the existing button and existing precedent for "verify what a command actually does before shipping it" (see the code-view finding above).
- [x] **Verified live in a real browser**, including catching and correcting a false alarm from my own testing: a first pass showed a completely blank canvas and missing toolbar, which would have been a serious regression — investigated with a longer-wait diagnostic before concluding anything, and confirmed via screenshots taken at increasing delays that the page (and the SDK's own toolbar) loads and renders correctly, just slower on that particular run than the fixed wait the test used; not a real bug. With adequate wait time: all 4 new labels render, the existing Add Blocks panel still opens correctly (proving the sidebar restructuring didn't disturb any existing button), the new Advanced panel opens with the code-view button, and clicking it genuinely opens the SDK's real "Import code" modal with a live textarea — not a stub. Zero console errors throughout.

### 3.4 Ask Ninja + AI section tools `[BE+FE]` ✅ done+verified
- **Design decision**: since `websiteData` is a backend-opaque GrapesJS blob with no "sections" concept (see the phase-level audit note), the backend doesn't try to identify "which section" itself. Instead the frontend sends whatever HTML is currently selected in the editor (or the whole page body if nothing's selected) plus a plain-English instruction; the backend asks OpenAI to return edited HTML for that same scope, and the frontend swaps the result back into the live GrapesJS component tree. AI Rewrite Section / AI Redesign Section / Ask Ninja's free-text mode are all the same backend endpoint with a different `mode` and instruction — not three separate integrations.
- **Styling constraint found and designed around**: this project's style manager writes class-based CSS (confirmed by the existing `previewWebsite` code already needing a dedicated `studio:projectFiles({styles:"inline"})` export step for the whole page) — a single component's `toHTML()` alone wouldn't carry its visual styling, and any new class name the AI invented would render unstyled once imported back (GrapesJS's CSS rule composer only tracks rules created through its own UI). Fixed by requiring the AI to output **inline `style` attributes only**, never classes or `<style>` tags — stated explicitly in the backend system prompt.
- [x] `[BE]` New `services/website-builder-service/src/utils/openaiService.js` (greenfield — this service had zero LLM wiring before), mirroring Phase 2's `ai-chat/utils/openaiService.js` shape (`getOpenAIClient` lazy singleton, same `openai` package). `editSectionHtml(html, instruction, mode)` on `gpt-4.1-mini`, mode-specific system instructions for `rewrite` (content only, keep structure) / `redesign` (visual style only, keep content) / `ask` (free-form, minimal edit).
- [x] `[BE]` New `POST /website-builder/ai/edit-section` — `WebsiteBuilderController.aiEditSection` (input length caps, ownership check, `checkLimit(token, 'ai_website_generation', true)`, calls `editSectionHtml`) + matching gateway route in `api-gateway/routes/web-builder.js`.
- [x] `[BE]` New quota feature key `ai_website_generation` added to `auth-service`'s `Plan.limits` and `Subscription.usage`/`allowedLimits` schemas, `seed-plans.js` values scaled like `web_builder_sessions` (free 0 / go+go_student 60 / pro 300 / business 1200 / custom unlimited), and a `FEATURE_NAMES` label — following the existing `checkLimit` pattern exactly as planned.
- [x] **Real bug found and fixed**: `Subscription.allowedLimits` is a fixed (non-`Mixed`) embedded schema, so Mongoose applies the schema default (`0`) for any key missing from an already-stored document **as soon as it's hydrated** — even though the raw stored document never actually contains that key. `checkUsageLimit`'s `subscription.allowedLimits[feature] !== undefined` fallback-to-current-plan check can therefore never see "missing" for an old subscription; it silently used the hydrated default of `0` instead of ever falling through to the plan's real limit. **Verified live**: a real `check-limit` call through the gateway for the demo account (business plan, meant to get 1200/mo per `seed-plans.js`) returned `limit: 0` before this was fixed. Fixed with a one-time backfill script (`services/auth-service/backfill-ai-website-generation-limit.js`) that uses `.lean()` (bypasses hydration defaults, so `$exists` checks the real stored shape) to set `allowedLimits.ai_website_generation` from the user's current plan wherever it's genuinely absent. **Re-verified live**: same call afterward returned `limit: 1200`. This bug class will recur for any future new quota key — the backfill script is the reusable fix pattern, not a one-off.
- [x] `[FE]` `WebBuilderService.aiEditSection()` + types.
- [x] `[FE]` `WebsiteBuilderStudio.tsx`: always-visible floating "✨ Ask Ninja" button (bottom-right, plain React overlay — same reliability reasoning as 3.5's save-status/History UI, and doubly appropriate here since feedback.md specifically asks for "always-visible," which an SDK sidebar button that needs a click to open can't really satisfy) opening a small panel with a free-text instruction box, a live "editing selected `<tag>`" vs. "editing the whole page" label (wired to real GrapesJS `component:selected`/`component:deselected` events, not just read once), and context-sensitive quick actions: **Rewrite Section** / **Redesign Section** buttons when something non-image is selected, **Regenerate Image** when the selection is an `<img>` or has a `background-image` style.
- [x] `[FE]` Selection-scoped HTML extraction (`extractHtmlForAiEdit`): tags the selected component with a unique temporary `data-ninja-ai-marker` attribute (via `getAttributes`/`setAttributes` — standard, stable GrapesJS Component API, not a guessed one), runs the same proven `studio:projectFiles({styles:"inline"})` export `previewWebsite` already uses, finds the marked element in the exported HTML, and strips the marker back off both the export and the live component afterward.
- [x] `[FE]` Result application (`applyAiEditResult`): removes the selected component and re-adds the AI's returned HTML at the same index under the same parent (`parent.components().indexOf/remove/add`), preserving its position among siblings, and re-selects the new component; falls back to `editor.setComponents(html)` for the whole-page case. Triggers `editor.store()` afterward so the change is saved immediately rather than waiting for the SDK's own autosave interval.
- [x] `[FE]` AI Regenerate Image reuses the **existing** `imageGenService.generateImage()` (already wired to `imaginative-service`'s Gemini-based generator, S3 storage, and its own `generated_images` quota) rather than building a second image pipeline — sets the result URL as the selected `<img>`'s `src`, or as `background-image` for a background-image element.
- [x] **Real bug found and fixed via live testing — whole-page edits were destroying the rest of the page.** First live test: asked Ninja (nothing selected) to change the main heading's wording. The response applied successfully and the toast said so, but the entire rest of the page (nav, About section, images, everything) was gone — only the new heading remained. Root cause: the "ask" mode's system prompt only asked the model to make "the minimal edit," which it took as license to return *just* the changed element rather than the complete page with that one part changed, and separately the flat `max_tokens: 4000` cap was too small to safely echo back a large page even if it tried. **Fixed with three layered changes in `openaiService.js`**: (1) the system prompt now explicitly requires the output to contain *every* element of the input, calling out that omitting untouched content is a critical failure; (2) `max_tokens` now scales with input size (`min(16000, max(2000, html.length/3 + 1000))`) instead of a flat cap, and a `finish_reason === "length"` truncation is now a thrown, user-facing error instead of silently returned partial HTML; (3) as defense in depth against the model dropping content even without hitting the token cap, a response shorter than 40% of the input's length (for inputs over 500 chars) is rejected with a clear error rather than ever reaching the frontend to apply. **Re-verified live end-to-end through the real gateway path** with the exact same instruction against a full multi-section demo page: response took 43.5s (well inside the gateway's 60s proxy timeout), the heading changed as asked, and every other section (nav, About Me, Exhibitions, Services, Pricing, Team, portfolio images) was confirmed still present in the live canvas afterward.
- [x] **Verified live in a real browser through the actual gateway path** (Playwright: login → wizard → editor → Ask Ninja), not just curl: (1) whole-page free-text edit — heading changed, rest of page preserved (see above), save-status badge showed "Saving…" confirming `editor.store()` fired, zero console errors; (2) selection-scoped **Rewrite Section** — clicked an `<h1>` in the canvas, confirmed the panel's live selection label and the Rewrite/Redesign quick buttons appeared, clicked Rewrite Section, got the success toast and the section updated in place; (3) **AI Regenerate Image** — selected an `<img>` (a placeholder SVG data-URI in the demo template), confirmed the Regenerate Image quick button appeared instead of Rewrite/Redesign, typed a prompt, got back a real S3-hosted image from `imaginative-service` and confirmed the canvas `<img>`'s `src` actually changed to it. All three flows also independently curl-verified through the gateway (including the `redesign` mode specifically, and the 400/404/401 error paths: missing fields, a non-owned website, no auth token — all return clean, non-leaking errors). **Redesign Section's live-UI drive** (originally deferred as "curl-verified only, judged adequate") was closed out on a later pass: selected an `<h1>` in a real editor canvas, clicked the Redesign Section quick action, got "Ninja applied your changes!" with zero console errors, and confirmed visually that the heading actually picked up a distinct visual treatment (a bordered/boxed redesign) while the rest of the "Verve.Digital" agency template stayed untouched.
- **Scope note**: "let users add, duplicate, delete and reorder sections" (the other bullet in this feedback.md area) was confirmed in the phase-level audit to already work via GrapesJS's own stock canvas/layer-manager interactions (drag from Blocks, the Layers panel, and the canvas toolbar's built-in copy/delete-selected-component controls) — not something Ask Ninja needed to add.

### 3.5 Auto-save, undo, version history `[BE+FE]` ✅ done+verified
- [x] `[FE]` Persisted "Saved"/"Saving…" indicator — auto-save itself already worked (SDK storage manager), it just had no visible state at all before. Hooked into the existing `saveToServer`/`handleSave` flow, shown as a small badge (rendered as a plain React overlay, not through the SDK's own declarative layout — see below for why) with a timestamp once saved.
- [x] `[BE]` New `WebsiteVersion` model (didn't exist at all — the old schema had one live `websiteData` per website, overwritten on every save, no recovery path). Snapshots the *pre-overwrite* state (so a version is a checkpoint you can go back to, not the state that just got saved over it), throttled to at most one automatic snapshot every 10 minutes so a long editing session with frequent autosaves doesn't flood the list, plus an always-forced snapshot on publish (a meaningful milestone) and before a restore (so restoring is itself undoable, not a one-way jump). Pruned to the most recent 20 versions per website.
- [x] `[BE]` New endpoints: `GET /versions/:websiteId` (lightweight listing — id/timestamp only, not the full data blob) and `POST /versions/:websiteId/:versionId/restore`, plus gateway routes for both.
- [x] **Real gap caught while wiring this up**: the `deleteWebsite` cascade-cleanup built in 3.6 predates this model and didn't know about it — deleting a website would have left its version snapshots orphaned in the DB forever. Added `WebsiteVersion.deleteMany({ websiteId })` to the delete flow.
- [x] `[FE]` Version history panel + restore button, plus the "History" trigger button — also a plain React overlay rather than an SDK layout entry (see below). Restoring reloads the page so the editor's existing `storage.onLoad` path picks up the freshly-restored data the same way it already does on a normal load, rather than trying to mutate the live SDK instance's loaded project in place.
- [x] **Why plain React overlays instead of adding these into the SDK's own toolbar** (consistent with 3.7's finding): the SDK's toolbar buttons render with no `title`/`aria-label`/accessible text anywhere in the DOM, confirmed by dumping every button's attributes during testing — reliably targeting a *new* SDK-layout button for anything needing dependable interaction (like a version-history trigger a user needs to find and click) wasn't worth fighting given that precedent, so both new UI pieces sit as an independent, easily-targetable overlay instead.
- [x] **Real bug found and fixed via live testing**: the overlay's first draft was positioned `top-2` (near the very top of the screen), which visually collided with the SDK's own toolbar row occupying the same vertical space — confirmed via a screenshot showing the new "History" pill overlapping the Desktop device selector. Moved to `top-14`, clear of the toolbar.
- [x] **Verified live in a real browser, including the throttle mechanics** (not just typecheck): via curl through the gateway first — saved a website twice, confirmed a version only appears after the *second* save (correctly snapshotting the first, non-empty state) and not the first (nothing to snapshot yet); saved a third time immediately after and confirmed no new version was created (throttle working); restored to the first version and confirmed both that the live data actually reverted *and* that a forced snapshot of the pre-restore state was created (2 versions after one restore, not 1). Then via Playwright: clicking the editor's actual save icon (found by screen coordinate after the SDK's toolbar again gave no accessible selector — text/title/aria-label targeting all failed, same as 3.7) shows "Saving…" immediately and "Saved &lt;time&gt;" ~6s later alongside the real "Website saved successfully!" toast, and the History panel opens correctly showing the accurate empty state on a fresh website. Zero console errors throughout.

### 3.6 Recent Websites + dashboard fixes `[BE+FE]` ✅ done+verified
- [x] Fixed the auth-hydration race condition causing false "No Websites Yet" (see 3.1 — same underlying fix, `WebBuilder.tsx`'s `useEffect` now waits on `user?.id`).
- [x] **Simplified the action set toward the spec's "Edit, Preview and Publish only"**: the card previously exposed 5 actions at once (Edit, Domain Settings, SEO Settings, Preview, Published Link). Domain Settings, SEO Settings, and Social Links — real but secondary/occasional — moved into a small overflow ("⋮") menu instead of extra buttons on the primary row; the primary row is Edit / Preview / Publish.
- [x] **Real one-click publish, not a relabeled link — built and verified live.** Originally the "Publish" button was just the existing "Published Link" button relabeled (view-only once already published, disabled otherwise) — instantiating the full Studio SDK editor just to publish seemed like it would need the same complexity as the wizard's editor hand-off. Turned out unnecessary: `WebBuilder.tsx` already had a `generateHTML(projectData)` helper for "Preview Staging" that spins up a **headless** plain-`grapesjs` instance (`container: document.createElement("div")`, never mounted to the page), loads the stored `websiteData`, calls `getHtml()`/`getCss()`, and destroys it — the exact mechanism needed, already proven working, just never reused for Publish. Wired the button to reuse that same helper, feed the result to the existing `publishWebsite` API, and show a `PublishWebsiteModal.tsx` confirmation first (mirrors `DeleteWebsiteModal.tsx`'s pattern, and the existing in-editor "Ready to Publish?" confirmation's wording). The status pill ("Published"/"Draft") now doubles as the "view live" action, freeing the Publish button to always mean "publish now." **Verified live in a real browser**: created a website via the wizard, saved once from the editor (to give it real `websiteData` to publish from), returned to the dashboard, clicked Publish, confirmed the modal, and got the real "Website published successfully!" toast with zero console errors — no editor tab ever opened for this.
- [x] **Dead code resolved**: `PagesManagerModal.tsx` was a complete, working component (pages list/create/rename/delete, all hitting real existing endpoints) that had simply been commented out — wired it back in via the overflow menu's new "Manage Pages" item rather than deleting working code. `AssetService.ts` confirmed genuinely dead (grepped: only self-referencing; its `/assets/*` backend routes never existed at all, not even before this pass) — deleted.
- [x] `[BE]` New `DELETE /:websiteId` endpoint (didn't exist anywhere before) — cascades cleanup rather than just removing the DB record: preview thumbnail, favicon, and *every* published page under the website's S3 prefix (not just the single stored `publishedLink` file — a multi-page site has more than one; added a new `s3Service.deletePrefix()` list+bulk-delete helper for this), plus any attached `Document` records and their on-disk files. Best-effort on storage cleanup — a partial S3/file failure doesn't block deleting the record itself.
- [x] **Real bug caught and fixed before it shipped**: initially registered the new `DELETE /:websiteId` route immediately after `/create`. On inspection this pattern only matches a single path segment (so none of the other DELETE routes — all multi-segment — were actually at risk), but registering broad catch-all params early is still the wrong default; moved it to the very end of both the backend route file and the gateway route file, after every more-specific DELETE route, as a defensive fix regardless.
- [x] **Second real bug caught via live testing, not assumption**: initially gated the new "Manage Pages" menu item behind `disabled={!hasWebsiteData}` (copying the Staging Preview button's pattern) — but `pages` is a separate array on the model, independent of `websiteData` (confirmed by reading the schema), so a freshly-created website with no saved editor content yet still has a valid default Home page and should be manageable. The gate made the button permanently disabled on any website that had never been opened in the editor, caught when a live browser test couldn't click it. Removed the gate — the original (disconnected) button never had one either.
- [x] **Verified live in a real browser**: created a website via the wizard, opened the overflow menu (confirmed Domain/SEO/Manage Pages/Delete all present), opened Manage Pages and confirmed the default Home page listed, then deleted the website via the confirmation modal and confirmed the card actually disappeared from the list (not just a success toast) — re-run from a clean slate after ruling out a false failure caused by leftover data from an earlier crashed test run.

### 3.7 Responsive preview `[FE]` ✅ done+verified
- [x] **Real inconsistency confirmed and fixed**: the dashboard's `PreviewModal` used mobile `420-600px` while the in-editor preview used `568-768px` for the same "mobile" device — same app, two different answers for what "mobile" means. A true single shared component isn't possible without a much larger change: the dashboard preview is a plain React component, the in-editor one is built through the GrapesJS Studio SDK's declarative `layoutToggle` layout schema (buttons/rows as data objects, not JSX) — so instead consolidated the one thing that actually mattered, the breakpoint *values*, into one shared `config/previewDevices.ts` constant imported by both.
- [x] **Verified**: `npx tsc --noEmit` and `npm run build` clean; the dashboard side (`WebBuilder.tsx`) exercises the same code path proven working in earlier phase testing.
- [x] **In-editor preview click-through closed out on a later pass.** Originally disclosed as unverified: the toolbar's icon-only buttons have no `title`/`aria-label`/accessible text anywhere in the DOM, and four targeting strategies (text, title/aria-label, DOM-position-relative-to-Publish, icon content) all failed. Resolved by dumping every visible toolbar button's exact screen position once (the same technique that separately found the save button for 3.5/3.9's own testing) and identifying the Preview Website button by its stable position (index 17, the play-triangle icon, directly left of Publish). **Verified live**: clicking it opens the real fullscreen preview overlay with working Desktop/Tablet/Mobile buttons; clicking Tablet resizes the preview container to exactly `768px` and Mobile to exactly `420px` — both matching `PREVIEW_DEVICE_SIZES` precisely, confirmed by reading the actual computed width, not just a visual glance. Screenshot also incidentally confirmed the new templates' pure-CSS hamburger-menu convention (3.2) renders correctly at the mobile breakpoint.

### 3.8 SEO/metadata/settings `[BE]` ✅ done+verified
- [x] **Confirmed and fixed the two-disconnected-SEO-surfaces bug with real evidence, not just code-reading** — published a real site with dashboard SEO settings set to one title/description and a simulated editor-exported page carrying a *different* title/description, then fetched the actual live HTML from S3. Found genuine duplicate `<title>`/`<meta description>`/`<meta og:*>`/`<link rel="icon">` tags in the output — the dashboard's values happened to "work" for the browser tab (browsers use the first `<title>` they see) but this is invalid duplicate-tag HTML, and search engines / social-share scrapers don't reliably prefer the first occurrence the way browsers do, so it wasn't actually reliable SEO despite looking fine in a quick browser check. Fixed `sanitizeAndInject()` in `WebsiteBuilderController.js` to strip any pre-existing title/meta/favicon tags from the incoming HTML *before* injecting the dashboard's `seoSettings`, making it the actual single source of truth rather than "usually first in document order." **Re-verified live**: re-published the same test case, fetched the new output, confirmed exactly one `<title>` and one `<meta description>` remain, both the dashboard's values.
- [x] **Corrected a wrong assumption from the initial audit before building anything**: contact forms aren't actually a gap — read `ContactForm.tsx`'s canvas-block code and found it already has a real submit handler posting to `/api/user/contact-email`, confirmed that route exists on both the backend and gateway. It works today, just configured per-form-instance (an owner email set in the block's properties) rather than as a site-wide setting. Not a stub, no fix needed — corrected the plan rather than building something already-working a second time.
- [x] **Social links settings panel — built and verified live.** Originally deferred (canvas-block-only, `SocialLinks.tsx`'s static placeholder `href="#"` links, no site-settings convenience to fill them in once); closed out on a later pass. New `socialLinks` field on `WebsiteBuilder` (facebook/twitter/instagram/linkedin/youtube/tiktok), `POST /update-social-links` endpoint + gateway route, and a `SocialLinksModal.tsx` (mirrors `SEOSettingsModal.tsx`'s pattern exactly) reachable from the dashboard's overflow menu. Applied at **publish time** via a new `injectSocialLinks()` helper alongside `sanitizeAndInject` — regex-matches each platform's `<a class="social-link {platform}">` and replaces its `href`, leaving any platform with no configured URL untouched (so a site that never sets social links publishes exactly as before). **Verified live end-to-end**: settings-panel save round-tripped through a real browser session (modal opens, saves, persists — confirmed via a direct API read-back); separately, published a page containing the actual `SocialLinks.tsx` block markup with Facebook and Instagram configured but Twitter/LinkedIn left blank, fetched the real live HTML from S3, and confirmed Facebook/Instagram got real hrefs while Twitter/LinkedIn correctly kept `href="#"` — the same fetch-and-verify methodology already established for the SEO duplicate-tag fix above.
- [x] **Custom 404 page — built and verified live.** Reused the *existing* multi-page publish/Pages Manager mechanism rather than adding a new model field or parallel system: a page with slug `404` published as `404.html` is now picked up automatically. Hooked into `serve_by_domain`'s existing S3-proxy-with-retry error handling (the code that already tries appending `.html` before giving up) — before falling back to the generic "File not found on S3" text, it now checks for a `404.html` at the site's S3 prefix and serves that instead, with a real `404` HTTP status (not `200`, which would be wrong for crawlers/link checkers). Deliberately scoped to `serve_by_domain` only (the custom-domain serving path, which already correctly proxies-and-can-intercept) — `get_published_website` (the default `/published/...` URL path) uses a blind `res.redirect` to S3 with no server-side existence check at all, a separate, pre-existing architectural gap not part of this specific fix. **Verified live**: connected and verified a `.local` test domain (auto-verify bypass already in the code, confirmed in the earlier audit), published both `index.html` and a distinctive `404.html`, requested a nonexistent path with that Host header, and confirmed the actual custom 404 content came back with a `404` status code, not the generic message.
- [x] **Major infrastructure bug found and fixed while investigating this** (unrelated to SEO specifically, but discovered here): the API gateway's shared `proxyRequest`/`proxyAuthenticatedRequest` helper (`api-gateway/utils/proxy.js`, used by every service's gateway routes, not just website-builder) blindly forwarded the client's original `content-length` header even though non-upload requests are re-serialized by axios from `req.body` (already parsed by `express.json()`). When a request body's re-serialization doesn't byte-match the original (e.g. pretty-printed/indented JSON, or any client not doing compact `JSON.stringify`), the stale forwarded length no longer matches the actual outgoing body — Node sends exactly the declared byte count, so the upstream server hangs waiting for bytes that will never arrive, until its own timeout fires. **Reproduced live**: a `publish-website` call that completed in well under a second when hitting the service directly took 70+ seconds and failed through the gateway, using the identical payload. Fixed by dropping the stale `content-length` for non-upload requests (upload requests stream `req` through raw/unchanged, so their original length stays valid and is left alone). **Re-verified live**: the same reproduction case now completes in 0.7s through the gateway. This is real, general gateway infrastructure — every JSON-body gateway route in the app benefits from this fix, not just website-builder's. It never surfaced in any of this session's extensive browser-driven testing because a real browser's own `axios`/`fetch` client always sends compact JSON that byte-matches its own re-serialization; it only reproduces with non-compact request bodies, which is how this was actually found (a pretty-printed JSON test payload from a file, not guessed).
- Also investigated and ruled out as a false alarm: suspected a frontend/backend field-name mismatch on multi-page publish (backend checks `file.path`, but the raw GrapesJS `projectFiles` SDK output only has `name`) — read `WebsiteBuilderStudio.tsx`'s actual `handlePublishWebsite` and confirmed it builds a separate `filesPayload: {path, content}[]` from the raw SDK output before sending, so the real request the frontend sends already matches what the backend expects. No fix needed; this was an artifact of my own ad-hoc test payload guessing the wrong shape, caught before it was written up as a bug.

### 3.9 Gateway/infra bug fixes found during audit `[BE]` ✅ done+verified
- [x] Added missing `GET /analytics/:websiteId` gateway route (previously 404'd).
- [x] Fixed `GET /gallery/:filename` vs gateway's `/gallery/:userId/:filename` path-segment mismatch (was broken through the gateway) — gateway now mirrors the backend's actual single-segment route exactly.
- [x] `SYSTEM_IP` env var is a non-functional `127.0.0.1` stub for A-record domain verification — deliberately not fixed here, flagged for Phase 4 (domains) since it's this phase's dependency but that phase's feature.
- [x] **Verified live via the actual gateway**: deleted a real website (exercising the new endpoint through both gateway route files) and confirmed the S3 cascade cleanup worked, not just the DB record.

### 3.10 Test matrix ✅ — every sub-phase individually verified live throughout; this section is the consolidated cross-cutting pass
Each of 3.1-3.9 already has its own detailed live-verification evidence recorded above as it was built (curl-through-gateway and/or Playwright-through-real-browser, per the phase-wide discipline). Rather than re-litigate each of those individually, this pass ran one **continuous real user journey through the actual browser + gateway**, exercising most of the phase's pieces together in the same session — the thing isolated per-feature tests can't prove on their own (that moving between them doesn't corrupt state, that the pieces actually compose).

- [x] **Full journey, one continuous Playwright session**: log in → open the wizard → fill business info, select "Hotel" industry → pick "Let Ninja choose for me" → confirms the new `coral-bay-resort` template (from 3.2) was correctly matched by industry, not a stale/wrong fallback → "Edit" opens the real editor → **Ask Ninja** (3.4) told to change the headline, applied successfully, toast confirmed → **save-status badge** (3.5) shows "Saved 01:25 PM" → **History panel** (3.5) opens and shows real content → **Publish** (3.5/3.8/3.9) clicked, confirmed, "Website published successfully!" toast shown → **delete** (3.6) via the dashboard's overflow menu path, cascade-cleanup confirmed (`200 {success:true}`). Zero console errors on both the wizard tab and the editor tab throughout. Final screenshot shows all of it together correctly: the new hotel template's real content (booking widget, amenities icons, nav), the Ask-Ninja-edited headline ("E2E Verified Resort"), the save-status badge, the History button, and the publish-success toast, all in one frame.
- [x] Every sub-phase's own dedicated verification (already itemized in 3.1-3.9 above) stands as-is: wizard step-by-step + smart-fill + logo/brand-color (3.1), editor sidebar restructure with a false-alarm regression correctly ruled out (3.3), auto-save/version-history throttle mechanics tested via direct DB inspection before the UI pass (3.5), dashboard/Recent-Websites race fix + delete cascade + real one-click publish (3.6), responsive preview breakpoints including the in-editor click-through (3.7), SEO duplicate-tag bug + social links injection, both fixed and fetch-verified via real S3 publishes (3.8), the gateway content-length bug found via A/B direct-vs-gateway comparison (3.9), and this pass's own template-matching (3.2) and Ask Ninja/AI-edit content-preservation bug (3.4) — each with its own root-cause narrative and re-verification, not glossed over.
- [x] **Cross-phase bugs this final pass caught that no single sub-phase's own testing would have surfaced**: the gateway's per-request `http.Agent`/`https.Agent` leak (3.2's write-up) only became visible under the sustained request volume this end-to-end pass (and the template-seeding verification before it) generated — every individual sub-phase's own testing had used far fewer requests in a row and never hit it.
- [x] **Follow-up closure pass — every previously-disclosed gap and deliberate cut closed out, not left as "good enough."** When asked whether the phase was genuinely 100% done, the honest answer at the time was no: five items were either deliberately deferred (social links settings panel, logo/brand-color fields, one-click dashboard publish) or verification-only gaps (the in-editor preview toggle's click-through, Redesign Section's live-UI drive). All five were closed on this pass and are now itemized as done+verified in their respective sub-phases above, each with its own live evidence — including finding the in-editor preview button at all (four prior targeting strategies had failed; the fifth, dumping every toolbar button's exact screen position, finally worked) and discovering the dashboard already had a headless-GrapesJS `generateHTML()` helper built for Preview Staging that made "real one-click publish" far lower-risk than initially assessed. Phase 3 is genuinely complete now, not "complete except for a known list."

---

## Phase 4 — Publishing & Domains `[BE+FE]` ✅ complete
(`feedback.md` §5 — depends on Phase 3's publish step existing)

**Audited before building**: domain connect/verify/disconnect already existed (`connectDomain`/`verifyDomain`/`disconnectDomain`, `DomainSettingsModal.tsx`/`VerifyDomainModal.tsx`) — feedback.md's checklist implies this phase starts from zero, it doesn't. `VerifyDomainModal.tsx` already had real guided-DNS-setup UI (CNAME/A record instructions with copy buttons), better than expected. What was actually missing: a real, working env-var wire-up for what the frontend shows vs. what the backend checks (see the bug below), any SSL mechanism at all for customer domains, and — found only by trying to click through the journey live — the domain-connection UI was gated behind the site already being published, backwards from feedback.md's own stated order.

- [x] **Real, launch-blocking bug found and fixed — domain verification could never actually succeed against a real deployment.** `verifyDomain()` read `process.env.SYSTEM_BASE_DOMAIN` / `SYSTEM_IP`, but only `VITE_SYSTEM_BASE_DOMAIN` / `VITE_SYSTEM_IP` were ever set in `.env` (the `VITE_` prefix is required for Vite to expose them to the frontend bundle, and `VerifyDomainModal.tsx` reads exactly those to show the user what DNS record to add). The backend check was therefore always silently comparing against hardcoded fallback defaults completely disconnected from what the user was actually told to configure. **Confirmed live, not assumed**: read the actual resolved env values directly (`SYSTEM_IP: undefined`, `VITE_SYSTEM_IP: "127.0.0.1"`) — in this dev environment the IP fallback happened to coincidentally match, masking the bug; the base domain didn't ("localhost" configured vs. "sites.startupninja.ai" hardcoded). In a real deployment where `VITE_SYSTEM_IP` is the real server IP, this would have made verification fail for every real customer, permanently. Fixed to check the non-prefixed var first (for a legitimate backend-only override) and fall through to the same `VITE_`-prefixed values shown to the user, instead of diverging from them. **Re-verified live**: full connect → verify round-trip through the real gateway, both the `.local` dev-bypass path and a real external-DNS lookup (`example.com`, correctly fails with an actionable message, not a crash).
- [x] **Real bug found and fixed — domain connection was gated behind the site already being published**, backwards from feedback.md's own explicit journey order ("Create → Preview → **Connect Domain** → **Publish** → Live"). Practically this also fights DNS reality: propagation can take anywhere from minutes to 48 hours, so a non-technical user benefits from starting that process *while still building*, not being blocked from even opening domain settings until after their first publish. Removed the `disabled={!isPublished}` gate on the dashboard's "Domain Settings" menu item.
- [x] **GoDaddy/Namecheap API integration — now built, real implementations, gated behind credentials that don't exist yet in this environment.** Originally deferred as "confirmed out of reach" since `.env` had no registrar credentials — correct at the time, but the user asked for this to be closed out properly rather than left as a permanent gap. Built real, structurally-correct provider integrations instead of a mock: new `registrarDnsService.js` (`website-builder-service`) with two provider implementations — GoDaddy (`PUT` to `/v1/domains/{domain}/records/{type}/{name}`, `sso-key` auth, sets CNAME `www` + A `@`) and Namecheap (fetches existing host records via `domains.dns.getHosts` first since `domains.dns.setHosts` is a full-replace API, merges in the new CNAME/A records, checks the XML response for `Status="ERROR"`). New `POST /auto-configure-dns` route (service → gateway → frontend service method), and a new registrar picker + "Set Up Automatically" button in `VerifyDomainModal.tsx`, sitting above the existing manual instructions behind an "OR SET UP MANUALLY" divider — the manual path is untouched, this is additive. `isProviderConfigured()` gates the whole feature on the relevant API credentials actually being present; new placeholder env vars added to `.env` (`GODADDY_API_KEY`/`_SECRET`, `NAMECHEAP_API_USER`/`_KEY`/`_USERNAME`/`_CLIENT_IP`), left empty per this repo's convention of never populating third-party secrets. **Verified live, precisely**: the graceful-fallback path — clicking "Set Up Automatically" with no credentials configured — was driven end-to-end through the real browser/gateway and returns a clear, actionable 400 ("isn't available right now — use the manual DNS instructions below instead"), with the manual DNS instructions still fully present and correct beneath it (confirmed by screenshot, not just the response body). The actual GoDaddy/Namecheap API calls themselves cannot be live-verified without a real account and real credentials — same category of limitation as the SSL work below, disclosed rather than hidden.
- [x] **Automatic SSL provisioning for customer-connected domains — built, following an explicit scope decision made with the user first**: the existing `nginx-proxy`/`certbot` setup only had static Let's Encrypt certs for the app's own 3 domains, nothing for arbitrary customer domains — no server block, no cert mechanism, nothing. This is real production infrastructure that categorically **cannot be live-verified in this environment** (no public IP, no real DNS, no ACME-reachable server to complete an actual Let's Encrypt handshake against) — flagged this plainly and asked how to proceed rather than either quietly skipping it or writing untested code without saying so; chosen approach was "build the real automation, disclose clearly that it's unverified."
  - [x] `verifyDomain()` now drops a `<domain>.pending` marker into a shared `SSL_QUEUE_DIR` the moment a **real** (non-`.local`) domain newly passes DNS verification (`domainSslService.js`) — decoupled as a file, not a direct call, since `website-builder-service` has no business running shell commands or holding Docker socket access itself.
  - [x] New `certbot/provision-domains.sh` (replaces the old renew-only entrypoint): loops, and for each pending marker runs `certbot certonly --webroot`, writes an nginx server block for that domain into a shared `custom-domains/` directory, reloads `nginx-proxy` via `docker exec` (needs Docker CLI + the socket mounted — the standard minimal-footprint way to let one container trigger an action in a sibling one), and moves the marker to `.failed-<timestamp>` on failure rather than retrying forever. **Also fixed a real pre-existing bug found while touching this**: the original renew loop never reloaded nginx after a renewal either — `certbot renew` with no `--deploy-hook` — so a renewed cert would only actually take effect whenever nginx happened to restart for some unrelated reason. Added the hook.
  - [x] `nginx.conf`: the port-80 server is now a catch-all (`server_name _`) instead of only the app's 3 own domains, so ACME challenges for *any* domain being provisioned actually get served; added `include /etc/nginx/custom-domains/*.conf;` for the dynamically-generated per-domain HTTPS blocks.
  - [x] `docker-compose.prod.yml`: new `certbot/Dockerfile` (base image + `docker-cli`, needed for the reload step), wired the `ssl-queue` and `custom-domains` shared volumes between `website-builder-service` ↔ `certbot` ↔ `nginx-proxy`, and the Docker socket mount.
  - [x] **What was and wasn't actually verified, precisely**: (1) the script's own logic — glob matching pending markers, exact `certbot`/`docker` command construction, generated nginx config content, the success/failure/no-retry file-state transitions — tested against mock `certbot`/`docker` executables standing in for the real ones; both the success and failure paths behaved exactly as designed, confirmed by inspecting the resulting files, not just exit codes. (2) The full `docker-compose.prod.yml` — validated with the actual `docker compose config` command (not a guess), confirming every new volume, env var, and `${SUPPORT_EMAIL}` substitution resolves correctly. (3) The full `nginx.conf` — validated with a real `nginx -t` inside a throwaway container, using real self-signed certs and a real sample generated custom-domain include file; came back with a clean "syntax is ok / test is successful".
  - [x] **Upgraded from mocked to a real, non-mocked ACME verification, using Let's Encrypt's own official test server (Pebble).** The user asked for the SSL automation to actually be verified rather than left as "built but unproven." A real handshake against real production Let's Encrypt isn't possible from this environment (no public IP, no real DNS) — but Pebble (`ghcr.io/letsencrypt/pebble` + `pebble-challtestsrv`) is Let's Encrypt's own ACME protocol test server, so this gets a genuine, non-mocked result instead of either faking it or leaving it unverified. Built a full throwaway harness in the scratchpad: Pebble + challtestsrv on an isolated Docker network, a `-defaultIPv4`-configured DNS stand-in (no need to register real domains), and the **real** production `certbot/certbot` image (the same `Dockerfile` this repo actually uses) — ran the **real, unmodified `certbot certonly --webroot` command** from `provision-domains.sh` against Pebble (only `--server`/`--no-verify-ssl` added, to point at the test ACME server instead of production) and it **successfully issued a real, well-formed X.509 certificate** (`issuer=CN=Pebble Intermediate CA`, 90-day validity, matching real Let's Encrypt's defaults). Confirmed the script's generated nginx server-block was syntactically correct, that the `docker exec ... nginx -s reload` trigger fired, and loaded the actual issued cert into a fresh real `nginx:stable-alpine` container, which passed `nginx -t` cleanly. Hit and fixed several real Pebble/challtestsrv config issues along the way (DNS port mismatch, IPv6 being preferred over the configured IPv4 target, TLS trust to Pebble's self-signed ACME endpoint) — all resolved using each tool's own documented flags, not workarounds. All test containers, images, and scratch files deleted afterward; nothing test-related was committed. **What still cannot be verified outside a real deploy, and why**: the real `docker exec` reload against the actual `startup-ninja-main-proxy` container name (Pebble test used a stand-in container), and an ACME handshake against real production Let's Encrypt with a real public domain (Pebble's CA is deliberately not trusted by real clients/browsers — that's what makes it a safe test server). Both are environment-dependent, not logic-dependent; the automation itself is now proven correct against the real certbot binary and a real ACME implementation.
  - [x] **Real, launch-blocking bug found and fixed — `nginx.conf`'s `api.startupninja.ai` HTTPS block pointed at the wrong certificate path.** It referenced `/etc/letsencrypt/live/dev.startupninja.ai/...` instead of its own domain — a leftover from the "remove dev.startupninja.ai server block" cleanup visible in recent commit history — which would have caused a hostname/cert mismatch for every API client in production. Fixed to reference `/etc/letsencrypt/live/api.startupninja.ai/...`, its own domain, matching every other server block's pattern. Left an explicit deployment note directly in the config: since nginx requires the cert file to exist on disk at startup (no lazy-loading), an operator must run the one-time `certbot certonly --webroot -d api.startupninja.ai ...` bootstrap command *before* deploying this change, or nginx will fail to start. **Verified with a real `nginx -t`**, not just read — caught and fixed a self-introduced editing mistake in the process (an `Edit` call that accidentally dropped the `server { listen 443 ssl; ... }` opening lines, which the very next validation pass caught immediately via a real syntax error), then re-validated clean.
- [x] **Real bug found and fixed — "Show domain connection, SSL and publishing status clearly" wasn't true.** Domain/SSL status was invisible unless you dug into the overflow menu; the dashboard card showed nothing. Added a small clickable badge on each card (domain name + "Pending DNS" / "Domain Verified", styled yellow/green) that jumps straight to the same modal the overflow menu's "Domain Settings" opens.
- [x] **Second, more serious real bug found live while verifying the above — `VerifyDomainModal` never actually showed its own success state.** `selectedDomainWebsite` (the prop driving which panel the modal renders) is separate React state, captured once when the modal opens; calling `onUpdate()` (`fetchWebsites()`) after a successful verification refreshed the card list behind the modal, but never touched this reference — so the modal kept rendering its "still pending, here's your DNS records" view forever, even though the success toast fired correctly and the DB was genuinely updated. Only visible for the domain flow specifically, since it's the only modal where the user stays watching an async status change in place (SEO/Social Links/Delete all close immediately after their action, so their equivalent staleness is never visible). Fixed by having `fetchWebsites()` also refresh `selectedDomainWebsite` from the freshly-fetched list whenever it's set.
- [x] **Third real bug found in the same live pass — the success panel auto-closed after 1.5s**, too fast to actually read the new SSL-provisioning explanation (see below) that's the whole point of showing it. Removed the auto-close; the user dismisses it themselves via "Done", consistent with every other modal in the app.
- [x] **Honest-messaging fix**: the success panel said "Website is Live!" immediately upon DNS verification — no longer accurate now that SSL provisioning is a real, separate, asynchronous step after DNS verification succeeds. Changed to "DNS Verified!" with an explicit explanation that a certificate is being issued automatically and the site will be reachable once that finishes, rather than implying it's already live.
- [x] **Actionable publishing errors, not generic failures**: `publish_website`'s catch-all used to return the same "Internal Server Error while publishing website." for every failure mode. Differentiated the two things that actually go wrong in practice — S3/storage errors and local-disk errors — into their own clearer, retry-oriented messages, falling back to a still-more-human generic message otherwise (never just "Internal Server Error").
- [x] **Verified live end-to-end through the real gateway + browser, all pieces together**: created a website, connected a domain (initially blocked by the publish gate — caught, fixed, re-verified), confirmed the DNS-record instructions render, verified DNS (`.local` bypass), watched the dashboard card's badge update to "Domain Verified" in real time, and confirmed the modal's own success panel now correctly shows the new honest message with a working "Done" button — all captured via screenshots at each step, zero console errors throughout, all test data cleaned up afterward.
- [ ] "Do not market 'publish in minutes'..." — deliberately deferred, tracked in Phase 8's positioning check as already planned, not this phase's job.

---

## Phase 5 — Ninja Sales data integrity `[BE+FE]` ✅ complete
(`feedback.md` §6)

**Audited before building**: read every ninja-sales-service controller (`pipelineController`, `dashboardController`, `projectController`, `leadController`, `proposalController`, `invoiceController`, `followupController`, `taskController`, `activityController`, `aiController`) and `utils/teamContext.js` end to end, and live-tested the demo account's real data through the real gateway before touching any code. Two things this changed about the plan: (1) the "$0/no-deals" bug as originally reported could not be reproduced — the demo account's pipeline and dashboard both correctly showed its one real deal ($200, Contacted) — so the fix below targets the actual, verified bug behind that symptom class instead of a guess; (2) a dedicated cross-tenant-leakage audit (5.2's biggest risk item) came back clean, redirecting effort toward real gaps that were found instead.

### 5.1 Pipeline $0 / no-deals bug — root cause found and fixed `[BE+FE]`
- [x] **Confirmed live, not assumed, that the reported symptom doesn't reproduce with current code**: read the actual resolved `getOwnerId(req)` against the real `Project.userId` in Mongo for the demo account — they matched. Queried the real `/pipeline/columns` and `/dashboard/stats` endpoints through the real gateway with a real JWT — both correctly returned the one real project ($200, Contacted stage). The JWT-shape/ownerId-mismatch and divergent-query hypotheses from the original diagnosis plan were both ruled out this way, not assumed away.
- [x] **Real bug found instead, matching the same symptom class — Hold and Closed-Lost projects silently vanished from the Pipeline board.** `pipelineController.js`'s hardcoded `pipelineStages` array (the Kanban's column list) only listed 7 of the schema's 9 valid `pipelineStage` values — a project moved to `hold` or `closed-lost` (both selectable in every stage dropdown across the app) would still be fetched by the query but then matched **no column at all**, so it just disappeared from the board with no error, no indication anything was wrong — indistinguishable from data loss to a user. The frontend's "On Hold" stat (`kanbanData.find(c => c.id === "hold")`) was permanently stuck at 0 for the same reason, since no such column ever existed in the response. Fixed by adding both stages to `pipelineStages`; `LeadsPipelinePage.tsx`'s `emptyColumns` fallback updated to match. Styled the Hold column as visually secondary (dashed border, "Non-primary" badge) per feedback's "Hold as non-primary" — cards land there instead of disappearing, but it reads as deliberately de-emphasized, not a primary pipeline stage.
- [x] **Verified live**: created a real lead/project/follow-up through the real gateway, confirmed it appeared in Pipeline; dragged it through the API to `hold`/`closed-lost` and back, confirming it now stays visible in both cases; screenshotted the real Pipeline board rendering all 9 columns including "Hold — Non-primary" and "Closed Lost". Test data cleaned up afterward.

### 5.2 Rest of §6
- [x] **One canonical deal/project source — confirmed by dedicated audit, no divergent queries found.** Every list/read/write in `pipelineController`, `dashboardController`, `projectController`, `leadController` and the AI controller derives its Mongo filter from `getOwnerId(req)` / `leadScopeFilter(req)` / `projectScopeFilter(req)` in `teamContext.js` — no controller queries a raw `{_id}` or trusts a body-supplied `userId`. The one structural nuance: Pipeline's drag-and-drop (`PUT /pipeline/move`) and Edit Project's stage field (`PUT /projects/:id`) are two different endpoints, but both write the same canonical `Project.pipelineStage` field on the same document — confirmed correct, not a bug, so left as-is rather than forcing an unnecessary single-endpoint refactor.
- [x] **Real, severe bug found and fixed — Lead Details' "Proposal & Invoice History" table was 100% hardcoded fake data.** Every single lead's detail page rendered the exact same three fabricated rows ("Initial Quote" $280,000, "Master Services Agreement", "Invoice #SN-102" $45,000) — never fetched from the backend at all, despite real, correctly-scoped `getProposals`/`getInvoices` endpoints already existing and already used correctly by `ProjectDetailsPage.tsx`. This is the more serious version of feedback's "never show another customer's proposal/invoice history" concern — not cross-tenant leakage (audit below found none), but literally fictitious data shown to every user. Fixed by wiring `LeadDetailsPage.tsx` to fetch real `getProposals({leadId})` + `getInvoices({leadId})`, combining/sorting them the same way `ProjectDetailsPage.tsx` already does, with a real empty state when a lead has no documents yet. **Verified live**: real proposal/invoice data (or the correct empty state) now renders in place of the fake rows, confirmed by screenshot.
- [x] **Cross-tenant data leakage — audited every handler in `proposalController`, `invoiceController`, `followupController`, `taskController`, `activityController`; none found.** Every ID-based handler authorizes in two layers: a server-derived `userId` (never client-supplied) baked into the query itself, plus `assertLeadAccessible`/`assertProjectAccessible` for the team-member-restricted case. `getInvoices`'s `projectId` query param was the one spot not run through `assertProjectAccessible` the way `leadId` already was (not a cross-tenant leak — still constrained by `userId` — but inconsistent with the rest of the codebase); tightened for parity.
- [x] **Core stages / Hold as non-primary** — see 5.1 above; Kanban now renders New → Contacted → Qualified → Proposal → Negotiation → Converted → Closed Won → Closed Lost, with Hold as a visually de-emphasized 9th column instead of an invisible one.
- [x] **Stage dropdown per pipeline card** — built. Each card now has a compact `<select>` (all 9 stages) alongside the existing drag handle, calling the same `movePipelineCard` endpoint as drag-and-drop.
- [x] **Instant metric refresh + toast on stage change** — built for both drag-and-drop and the new dropdown: optimistic UI update, real API call, success toast naming the destination column, and — new — automatic rollback to the pre-move snapshot with an error toast if the API call fails (previously a failed move silently left the UI in a state that didn't match the database, with no rollback and no toast at all). **The failure path itself was live-verified, not just written and assumed**: forced a real move request to fail (`page.route` intercepting the exact request Playwright's own browser sent, returning a real 404) against a throwaway deal, confirmed the error toast rendered with the actual server message and the card's dropdown reverted to its pre-attempt stage — not inferred from reading the code.
- [x] **Edit Project stage editing already in sync with Pipeline** — confirmed by the canonical-source audit above; no divergence existed, nothing to fix.
- [x] **Split "Create Lead & Project" into separate Create Lead / Create Deal for Existing Lead flows.** Audited first: `LeadsTable.tsx`'s own "Add New Lead" button was commented out, and the Leads page's header button silently opened the combined project-creation modal instead — meaning there was **no way to add a lead without being forced through required project fields** (Project Name was mandatory to submit). Fixed by building two new, focused modals: `CreateLeadModal.tsx` (lead-only fields — name, email, phone, company, job title, source, status, notes, decision-maker; no project required) now opens from the Leads page's "New Lead" button, and `CreateDealModal.tsx` (deal-only fields for one already-known lead, no lead search/creation) now opens from a new "Add Deal" button on `LeadDetailsPage.tsx`. The existing combined modal (`AddProjectModal.tsx`, lead-search-or-create + deal fields in one) is kept for the Projects/Pipeline pages' "New Deal" buttons, where creating-a-deal-with-an-optionally-new-lead is exactly the right flow — relabeled from "New Lead & Project" to "New Deal" / "Create Deal" so its button text now matches what it actually does everywhere it appears.
- [x] **Delete controls for leads and projects, real soft-delete + restore, cascade explained — built end to end.** Audited first: backend `deleteLead`/`deleteProject` endpoints already existed but were **dead code** — zero call sites anywhere in the frontend, no delete button on any list, detail, or edit screen. Both were hard deletes (`findOneAndDelete`) with no cascade handling for linked projects/tasks/follow-ups.
  - [x] `[BE]` Added `deletedAt`/`deletedCascadeFrom` fields to `Lead`, `Project`, `Task`, `FollowUp`; `teamContext.js`'s `leadScopeFilter`/`projectScopeFilter`/`assertProjectAccessible` now exclude soft-deleted records everywhere by construction (one change protects every controller that already funnels through them, rather than patching each query individually).
  - [x] `[BE]` `deleteLead` now soft-deletes the lead and cascades to its active projects (and each project's follow-ups) and tasks in one batch, tagging every cascaded record's `deletedCascadeFrom` with the lead's id; `restoreLead` reverses exactly that batch (and no other independently-deleted records). `deleteProject`/`restoreProject` do the same one level down for follow-ups. **Proposals and invoices are deliberately left untouched by cascade** — they're financial records that should survive their lead/project going away, and stay reachable from the Proposals/Invoices lists; this is explained in every delete confirmation dialog's copy.
  - [x] `[BE]` New `GET /leads/deleted`, `GET /projects/deleted`, `POST /leads/:id/restore`, `POST /projects/:id/restore` routes (service + gateway).
  - [x] `[FE]` Delete buttons (with a real confirmation dialog explaining exactly what gets moved to trash and what doesn't) added to the Leads list, Projects list, Lead Details, Project Details, Edit Lead, and Edit Project screens — restricted to owner/manager, matching the backend's existing permission rule. New `TrashModal.tsx` ("Trash" button in both list-page headers) lists deleted leads/projects with one-click Restore.
  - [x] **Verified live, precisely, end to end through the real gateway**: created a real lead + project + follow-up, confirmed it in Pipeline; deleted the lead and confirmed the response reported the correct cascade count (`{projects: 1, tasks: 0}`); confirmed the project now 404s and disappeared from Pipeline; confirmed both lead and project appeared in their respective `/deleted` list endpoints; restored the lead and confirmed the project came back (200, `deletedCascadeFrom: null`), its follow-up was restored too, and it reappeared in Pipeline. All test data permanently removed afterward. Also confirmed via screenshot that the Trash UI, delete confirmation copy, and the real "Lead Deleted"/"Lead Restored" activity-timeline entries all render correctly.
- [x] **Remove misleading $0/empty/loading flash — audited, no fake-placeholder pattern found.** Every list/stat view fetches real data and shows a real loading spinner while it's in flight (not a fake `$0` placeholder that later corrects itself); create/update flows call the real list endpoint again on success rather than rendering an optimistic guess. Nothing to fix here — the architecture already avoids this failure mode.
- [x] **Empty states** — `LeadsTable.tsx`/`ProjectsTable.tsx` upgraded from plain text-only rows to icon + message + actionable hint (matching the pattern the Pipeline Kanban and Tasks panel already used correctly); the rest of the app's empty states (follow-ups, AI suggestions, tasks) were already reasonable per audit and left as-is. **Actually seen rendered, not just read in code**: since the demo account always has one real lead/project, reaching a genuine zero-items state required using Phase 5's own soft-delete — deleted the demo lead (cascading to its project) through the real API, screenshotted Leads, Projects and Pipeline all correctly showing their new empty states with zero'd-out stats, then restored it back through the real API and confirmed every field (including its pipeline stage) came back exactly as it was.
- [x] **Extended lead fields**: audited what already existed on the Lead model/form (`source`, `notes`, `leadStatus` as the stage-equivalent — all present) vs. what feedback asked for but was missing: `value` and `next action` didn't exist at the lead level at all (only on Project, which is one-to-many with Lead, so a lead-level estimate is a genuinely different, useful concept, not a duplicate), and `lastContactAt` existed but was display-only, never editable. Added `estimatedValue` and `nextAction` to the `Lead` schema and `Edit Lead` form, and made `lastContactAt` a real editable date field.
- [x] **AI lead score (Hot/Warm/Cold + reason) — built, real, verified live.** New deterministic heuristic (`utils/leadScoring.js`) scores a lead from real signals (lead status, decision-maker flag, active deal stage/value/priority/confidence, overdue follow-ups) into Hot/Warm/Cold — kept as a fast non-AI computation (not a per-lead OpenAI call) so it's cheap enough to run across an entire account's lead list, matching the existing pattern already used by `postFollowUpSuggestions` for the same reason. `POST /ai/lead-score` layers one real AI-generated sentence on top explaining the score in natural language (with a template-based fallback if OpenAI is unavailable, same graceful-degradation pattern already used elsewhere in this controller). Shown as a badge + reason in `LeadDetailsPage.tsx`'s Lead Overview card. **Verified live** with a real OpenAI call through the real gateway (real reason text returned, not a stub).
- [x] **"Who should I contact today?" — built, real, verified live, including its trigger conditions.** New `POST /ai/contact-today` ranks the account's active leads by the same heuristic score, surfacing only Hot/Warm leads with a genuine trigger — an overdue follow-up, or no contact logged in 7+ days — not just "any warm lead," so it stays actionable rather than noisy. New `WhoToContactToday.tsx` widget on the Ninja Sales dashboard. **Verified live**: confirmed empty for the demo account (correctly — its one lead had neither trigger), then confirmed the staleness trigger fires correctly by temporarily backdating the demo lead's `lastContactAt` 10 days and re-querying (correctly appeared with the right reason text), then restored the original value.
- [x] **Per-account email sending wired into Follow-ups (and everywhere else it was needed) — audited and closed.** Audit found two real gaps: `AIOutreachAssistant.tsx`'s Send button was permanently disabled (`title="Connect your email provider to send from here"`), and `LeadDetailsPage.tsx`'s AI Suggestions drafting panel only offered Copy, no way to actually send. Both now send for real through the account's own configured SMTP (never the platform's), via a new shared `sendOutreachEmail` primitive in `documentEmailService.js` and `POST /ai/outreach-send`. Missing-SMTP is handled the same way in both places — an actionable toast linking straight to Ninja Sales → Email Sending — rather than a dead-end error. A successful send also logs a real Activity entry and refreshes the lead's `lastContactAt`, so it feeds back into the lead-scoring/staleness logic above rather than being a dead-end action. Verified live end-to-end (real SMTP send from both surfaces, Activity + `lastContactAt` confirmed in Mongo, test data cleaned up).
- [x] **Reminders delivery mechanism — built, real, verified live.** Previously disclosed as not built. Now: an hourly `setInterval` sweep (`workers/reminderScheduler.js`) checks every account once per calendar day (idempotency enforced by a unique-indexed `ReminderRun` lock, not by trusting the interval alone) for two real triggers — an incomplete follow-up whose due date has arrived (due or overdue, any lead temperature), or a **Hot** lead with no logged contact in 7+ days (same staleness rule as "who should I contact today," reusing `computeLeadScore`). When an account has anything actionable it gets both an in-app notification (written into the same shared `notifications` collection the existing bell UI already polls — zero frontend changes needed there) and a digest email sent via the **platform's own SMTP** (a fresh `platformMailer.js`, deliberately separate from the account's client-facing `EmailSettings` — this is Startup Ninja notifying its own registered user, not a client-facing send, so platform SMTP is the correct choice here, not a regression of the earlier "never send client mail from our SMTP" rule). Owner/manager can also see today's flagged leads and trigger an on-demand check via a new "Check In Now" banner on the Follow-ups page (`GET/POST /reminders/preview|run-now`, gated the same owner/manager-only way as Email Settings). **Verified live**: created a throwaway overdue follow-up, confirmed `/reminders/preview` detected it, ran `/reminders/run-now` and confirmed a real `notifications` document was written and a real digest email sent through platform SMTP, confirmed the Follow-ups page banner and its button render and work correctly in the browser (screenshotted both the "all caught up" and post-send toast states) — then deleted all test data. **Disclosed limitation, matching the original Phase 5 note**: cadence is fixed (hourly check, once-daily send per account) and there's no per-account opt-out/preference UI yet — flagged here as likely needing separate product input, not silently left out.

---

## Phase 6 (P1) — Social Pro, Image Gen, Ninja Legal, Onboarding/Settings/Admin

Start only after Phase 0-5 (all P0) are done — `feedback.md` explicitly says P1 follows immediately after P0, not in parallel with it, to avoid spending launch week on unrelated features.

### 6.1 Social Pro `[BE+FE]` ✅ complete (scope disclosed below)

**Audited before building** (two parallel deep-dive audits, backend and frontend, mirroring Phase 5's method): unlike the Ninja Sales CRM, this feature was NOT fabricated-data theater — OAuth, posting, and a real BullMQ+Redis scheduler all genuinely call live platform APIs. But the audit found concrete bugs and several checklist items genuinely unbuilt. Two scope decisions were made explicitly with the user before building: (1) "video media support" → a real AI Reel/video **script** generator, not full video-file upload/publish infra, since Instagram/Facebook (the platforms with real video APIs) aren't even reconnected yet — building video infra for disabled integrations would be backwards; (2) carousel/multi-image support was scoped to **LinkedIn and X only** — the two platforms actually reachable in the UI today, for the same reason.

- [x] **Security bugs found and fixed while auditing "restore connections"** (not on the original checklist, but blocked trusting anything else here):
  - LinkedIn's OAuth callback had no auth guard and no real state-nonce check — anyone could complete their own LinkedIn OAuth and link it to another user's account via a forged callback request (CSRF/account-linking). Fixed to match Instagram/Facebook's already-correct pattern: callback now requires `authenticateToken`, and the linked account is always `req.user`, never a client-suppliable value from `state`. Verified: route now 401s without a session; the popup-relay flow (`public/auth/linkedin.html` → authenticated `apiClient` call) already carries the JWT, so nothing broke.
  - Token encryption (`SocialMediaToken.js`) silently degraded to base64 (encoding, not encryption) on any error, invisibly. Fixed to fail loudly instead, and the `ENCRYPTION_KEY` validity check is now fatal at boot rather than a warning.
  - Facebook Graph API calls were pinned to v18.0 (shipped May 2023, past Meta's ~2-year support window — confirmed via live search that v19.0 was already sunsetting and v18.0 was long gone). Bumped default to v25.0 (current, longest runway) in code and both env templates.
  - Scheduled Instagram posts were silently routing customer images through catbox.moe (a public, anonymous, third-party host) instead of the account's own S3, because the worker dropped the S3 URL that real-time posts already used correctly. Fixed by threading it through.
  - Twitter/X's Connect button never rendered anywhere — the real, complete OAuth 1.0a implementation existed but the frontend never seeded a `twitter` entry into the connected-accounts list, so it was unreachable dead code. Fixed (one missing array entry); X is now a real, working, reachable connection alongside LinkedIn.
  - `ensureUserId()`'s client-suppliable `userId` fallback (dead code today, but a live footgun) removed — every call site already sits behind `authenticateToken`.
- [x] **Connect Instagram/Facebook — now fully restored and enabled.** Initially left as correctly-disabled "Coming Soon" placeholders (real code intact, deliberately not exposed) — per explicit follow-up instruction, both were re-audited for deprecated API usage and then turned back on for real. Audit result: Instagram's integration already used the *current* Meta-recommended flow (Instagram API with Instagram Login via `graph.instagram.com`, `instagram_business_*` scopes) — not the deprecated Instagram Basic Display API (killed Dec 2024) — confirmed via live research, so no migration was needed there. Facebook's classic Login + Graph API + page-permission scopes (`pages_manage_posts`/`pages_read_engagement`/`pages_show_list`) are likewise still current, not deprecated. One real piece of dead/dangerous code was found and removed while restoring Instagram: `uploadImageToHost()`, a fallback that routed images through catbox.moe — confirmed dead in every call path after the earlier S3-threading fix, deleted outright rather than left as a landmine, and `postToInstagram` now requires a real S3 URL instead of silently degrading. Frontend: `AccountsCard.tsx`, `PlatformTags.tsx`, and `SchedulingOption.tsx` had their Instagram/Facebook logic fully built already, just commented out — uncommented and wired back in (status checks, connect/disconnect, Facebook page selection/sync/removal, OAuth callback toasts). **Verified live**: both platforms' real Connect flow generates a valid auth URL and the resulting popup reaches Meta's actual login page (`facebook.com/v25.0/dialog/oauth` and `instagram.com/oauth/authorize` respectively, with real, accepted app credentials) — as far as this can be safely verified without completing a real OAuth consent with live credentials.
- [x] **Post creation: captions + hashtags — real, improved.** Real AI caption generation already existed (OpenAI, fine-tuned model) but had zero hashtag logic; a better, platform-aware hashtag/character-limit implementation existed fully built but was dead, unused code (`geminiService.js`, never imported anywhere). Ported its platform guidance (per-platform hashtag strategy, character-limit-aware trimming at sentence/word boundaries) into the live caption path, then deleted the dead file. Verified live through the real gateway with real OpenAI output.
- [x] **Carousel — built for real, all four platforms.** Post schema gained an `images[]` array (alongside the legacy single `image` for back-compat); Multer configs on every platform's post route and the scheduler route switched from `.single()` to `.array(..., 4)` (4 = Twitter's real per-tweet image cap, used as the universal limit). `linkedinService`/`twitterService` extended to upload/register multiple images and build a real multi-image share (LinkedIn's `ugcPosts` media array, Twitter's `media_ids` array). Initially scoped to LinkedIn/X only since Instagram/Facebook were still disabled; extended to both once they were re-enabled — Instagram now builds a real `CAROUSEL` container (per-image item containers + one parent container, Meta's actual multi-image API, not a workaround), Facebook now uploads each photo unpublished and attaches them all to one `/feed` post via `attached_media` (Facebook has no single carousel endpoint — this is the real, correct multi-photo pattern). All four backward-compatible with a single image. Frontend `FileUpload.tsx` rebuilt from a hard single-file limit to a real up-to-4 thumbnail grid (individual remove, "Cover" badge, "Add more" tile, cap enforcement with a toast). **A real gateway bug was caught during verification and had to be fixed twice** (once for LinkedIn/X, then again for Instagram/Facebook when they were enabled): the gateway's own separate Multer layer (a second parsing step between frontend and service) was still hardcoded to the old single-field name and silently 500'd every multi-image request — found and fixed across all five affected gateway routes. **Verified live end-to-end for all four platforms**: real 2-image multipart requests correctly uploaded to S3 and reached each platform's real "account not connected" boundary with a clean error, not a crash.
- [x] **Video media — real upload + real Instagram Reels/Facebook Video publishing.** Initially scoped to an AI script generator only (see content repurposing below), then upgraded to full video support once Instagram/Facebook were re-enabled and the user asked for the real thing. Post schema gained a `video` field (mutually exclusive with `image`/`images` — a post is either a carousel or one video, matching real platform semantics). Instagram/Facebook's Multer configs switched from `.array()` to `.fields([{images}, {video}])` so one route accepts either shape (100MB cap, up from 10MB); `FileUpload.tsx` now accepts video files, shows a real `<video>` preview with controls, and enforces the same either/or rule client-side ("Remove your images first to attach a video" / vice versa) so the UI can't produce a request the backend would reject. **Instagram**: real Reels Content-Publishing flow — `media_type=REELS` + `video_url`, then polls the container for up to 5 minutes (video processing is much slower than a photo, the existing 60s image timeout would have failed most real Reels) before publishing. **Facebook**: real Video API (`/​{page-id}/videos` with `file_url` — Facebook fetches directly from S3 rather than re-uploading the buffer through our server, which matters once files get large). Deliberately scoped to Instagram/Facebook only, matching feedback's own Reel-specific framing — LinkedIn/X video upload (a properly different, chunked-upload flow on Twitter's side) stays out of scope. Scheduled video posts are also out of scope for this pass — video ships through "Publish Now" only; the generic multi-platform scheduler wasn't retrofitted for a second media type. **A real bug was caught during verification**: the initial implementation wrapped the platform-post call in the same try/catch as the S3 upload and reclassified *any* error — including a legitimate "account not connected" — as a generic S3 failure, which would have quietly misreported real posting errors (rate limits, content-policy rejections, etc.) as storage problems in production. Found via a live test (Facebook returned "Failed to store media in S3" instead of "account not connected"), fixed by moving the platform-post call outside the S3-only try/catch, and confirmed correct afterward for both platforms. Verified live end-to-end: real video files uploaded to S3, presigned correctly, and both platforms' post calls reached their real "not connected" boundary cleanly.
- [x] **Scheduling/publishing — already real, one real bug fixed.** Genuine BullMQ+Redis queue and worker, actually calling live platform APIs and updating real post status (confirmed, not assumed, during the audit). The catbox.moe image-leak bug above was the one concrete defect found in this path. "Save as draft" was also found to be a fully wired, clickable, do-nothing button (the same "unfinished interaction" anti-pattern feedback explicitly calls out) — built for real: new `status: 'draft'` Post state, save/list/delete all work, and two real crash bugs it would have triggered elsewhere (`STATUS_META[status]` throwing on the new status in two different components, plus a pre-existing infinite-spinner-on-failed-fetch bug on the post-details page) were caught and fixed in the same pass.
- [x] **Analytics + visual content calendar — both built for real, real engagement metrics added.** Content calendar: new backend date-range query (`startDate`/`endDate` on the existing list endpoint) plus a new real month-grid `ContentCalendar.tsx` — fetches real posts for the visible month, renders status-colored dots per day, click-through opens the existing post-detail modal. Analytics: new `GET /scheduler/analytics` aggregates this account's own real Post documents — status/platform breakdown, a daily posting-activity trend, and a published-vs-failed success rate. Originally shipped with no engagement numbers (deliberately, to avoid fabricating data); **since upgraded to real likes/comments/shares** for the most recent 20 published Facebook/Instagram posts each — Facebook via the `pages_read_engagement` scope already in use (no new permission needed), Instagram via a newly-added `instagram_business_manage_insights` scope (costs nothing to add now since no real account had connected yet). Bounded to 20 posts per platform and fetched in parallel to stay fast; a permission or per-post fetch failure is skipped silently rather than failing the whole analytics call. Frontend shows an engagement card only when `postsCounted > 0`, never a fabricated zero. Verified live: confirmed the field shape end-to-end and that it degrades to zero cleanly with no connected accounts.
- [x] **Content repurposing + brand voice — built for real, verified live with a real OpenAI call.** No brand-voice data model existed anywhere in the backend before this. New `BrandVoice` model (business name/industry/tone/audience/keywords, scoped to Social Pro specifically — not the larger cross-product onboarding profile from §6.4, which is separate, larger, unstarted work) plus `POST /ai/repurpose`: one idea in, six platform-shaped outputs out (Instagram, Facebook, LinkedIn, X, a Reel/video script, a blog opening) in one structured OpenAI JSON-mode call, written consistently in the account's saved brand voice when one exists. New `ContentRepurposer.tsx` UI (collapsible brand-voice form + input + six labeled, copyable result cards). **Verified live**: saved a real brand voice, ran a real repurpose call, confirmed all six outputs were genuinely differentiated per platform (hashtags placed correctly for Instagram/X, none for Facebook, a real numbered shooting script for the Reel output, correct 280-char-safe X copy) and consistently reflected the saved brand voice, screenshotted rendering correctly, then deleted the test data.

- [x] **Follow-up: scheduled video posts + LinkedIn/X video, both built for real.** Real LinkedIn video posting added (the current Videos API: initialize upload → upload byte-range parts → finalize → poll until `AVAILABLE` → UGC post referencing the finished asset, verified against live current docs rather than the deprecated Assets API) and real X/Twitter video posting added (the classic v1.1 chunked INIT→APPEND→FINALIZE→STATUS flow over OAuth 1.0a — deliberately not the newer v2 media API, which needs OAuth 2.0 user tokens this app's 3-legged OAuth 1.0a flow doesn't produce). Both wired into "Publish Now" (new `.fields([{images},{video}])` multer configs on their routes/gateway proxies, mirroring Instagram/Facebook's existing pattern) and, critically, into the scheduler: `Post.js` already had an unused `video` field, but `schedulerController.js`'s schedule-creation and `postSchedulerWorker.js`'s job processor only ever handled `image`/`images` — extended both, and the worker now calls the exact same real posting functions (`postReelToInstagram`, `postVideoToFacebook`, the new `postVideoWithText`/chunked-upload functions) the immediate-publish path already used, rather than duplicating logic. **Verified live**: attached a real test video, confirmed LinkedIn/X/Instagram/Facebook are all now selectable together with a video (previously gated to Instagram/Facebook only), confirmed Publish Now reaches each platform's real API boundary with a clean "account not connected" error (not a 500), and confirmed scheduling a video post for all 4 platforms in one request correctly creates `status: "scheduled"` Post documents with a real populated `video` field and empty `images` (mutual exclusivity enforced) — then confirmed the worker's new video branch actually fires and reaches the same clean not-connected boundary when the job runs. LinkedIn/X's real chunked-upload HTTP calls themselves weren't exercised against their servers, since this dev environment only has placeholder LinkedIn/X credentials — same disclosed limitation class as Meta's app review below, not a gap in the code.

**Disclosed remaining scope** (not silently claimed done): **Meta app review has not been completed** — both Facebook's and Instagram's OAuth flows are verified working up to a real login page with valid, accepted app credentials, but production traffic beyond the app's own test users/roles requires Meta's app review process (2–4 weeks per Meta's own current guidance) before the integrations can serve real end users at scale. This is a Meta business-process step, not a code gap, and isn't something a code session can complete on its own — nothing left here is a code gap that was silently skipped.

### 6.2 Image Generation `[BE+FE]` ✅ complete (scope disclosed below)

**Audited before building** (backend `services/imaginative-service` + frontend `ai-image-gen`, same method as 6.1): a small, working Gemini 3 Pro Image ("Nano Banana Pro") generator with a basic gallery — no presets, no brand assets, no editing, no versioning of any kind existed. All six checklist items were genuinely unbuilt, not just disabled.

- [x] **Product naming fixed.** "Imagenative Ninja" → "Imaginative Ninja" in the page title (`AIImageGen.tsx`) and an admin-view label (`GeneratedImagesListView.tsx`, which also hardcoded that string as a fake "Model" value — changed to show the real `modelUsed` instead). Grepped both repos for every remaining instance; none left.
- [x] **Image quality/text rendering — real prompt-engineering pass, verified visually.** Added an explicit text-rendering instruction block (correct spelling, crisp/legible typography, accurate kerning) to every generation and edit prompt — Gemini 3 Pro Image is materially better at in-image text than most generators, but only when told the wording must be exact. Verified live: a generated "GRAND OPENING" poster rendered both words perfectly spelled and legible.
- [x] **Presets + aspect ratios — built for real.** 8 named presets (Instagram Post/Story, Poster, Advertisement, Website Hero, Product Image, YouTube Thumbnail, Social Graphic), each mapping to one of the 5 supported aspect ratios plus format-specific prompt guidance (e.g. a Story keeps the subject clear of the top/bottom UI-chrome zone, a thumbnail stays legible at small size). New "Preset" dropdown in `CreateImages.tsx` auto-sets the aspect ratio; "Custom" keeps manual control.
- [x] **Brand asset storage — new model, real S3-backed uploads.** New `BrandAsset` model (one profile per user): logo, up to 10 hex colors, up to 5 fonts, up to 8 product images, free-text visual-style notes. New collapsible `BrandAssetsPanel.tsx` (matches the Social Pro brand-voice UI pattern) with real logo/product-image upload, replace, and remove. An "Apply my brand assets" checkbox appears once assets are saved — when checked, the saved logo/product photos are sent to Gemini as real reference images (not just described in text) alongside the brand-color/font/style guidance, so generations can match the actual logo instead of inventing one. Verified live: uploaded a real logo, generated with it applied, confirmed `brandAssetsApplied: true` end-to-end.
- [x] **Iterative editing — real multimodal Gemini editing, not a re-generation trick.** The existing image is sent back to Gemini as an inline reference alongside a natural-language edit instruction (`editWithGemini` in `aiHelper.js`, same image-capable chat model used for generation) — genuinely edits the specific image rather than generating a fresh one from the text prompt alone. Exposed as "Continue Editing" in the image detail modal. Verified live: asked for a background color change, got back the same subject with a correctly changed background.
- [x] **Continue Editing, Create Variation, Compare Versions, version history, gallery, export — all built for real.** `GeneratedImage` gained a lineage schema (`parentImageId`/`rootImageId`/`versionNumber`/`versionType`/`editInstruction`) — every edit or variation is a new document linked to where it came from, not an in-place overwrite, so nothing is ever lost. New endpoints: `POST /edit/:imageId`, `POST /variation/:imageId` (one-click creative variation, same editing pipeline with a generic instruction), `GET /versions/:rootImageId` (full lineage for history + compare). The gallery (`RecentImages.tsx`) now lists one card per lineage (not every edit) with a version-count badge; opening a card shows a version-history thumbnail strip, a "Compare Versions" side-by-side view, and Continue Editing/Create Variation actions, all live-verified working end-to-end with real ~20–100s Gemini calls. Deleting a root image cascades to delete every version derived from it (both from S3 and the database) rather than leaving orphaned versions behind — a real bug class avoided, not just a missing feature. "Export" is the existing real S3 download, now working identically for every version.

- [x] **Follow-up: delete a single non-root version, built for real.** The backend (`imageController.js`'s `deleteImage`) already correctly handled this at the data level — deleting a root cascades its whole lineage, deleting a non-root removes just that one document — the gap was purely that the frontend's version-history strip never exposed a control for it. Added a hover-revealed delete icon to each non-root version thumbnail specifically (confirmed via `parentImageId` — the root thumbnail in the same strip still has no individual delete control, root deletion stays exclusively the existing whole-lineage action). **Verified live**: generated a real image, created a real edit (2-version lineage), deleted the non-root version via the new control with confirmation, confirmed it disappeared from the strip with no page reload, the view correctly fell back to the root, and Compare Versions correctly disabled again. A real bug was caught and fixed during verification: the confirmation dialog was initially nested inside the detail modal, so a backdrop click on it could bubble up and close the whole detail modal — fixed by rendering it as a sibling instead.

**Disclosed remaining scope**: presets and aspect ratios are still limited to Gemini's supported set (1:1, 16:9, 4:3, 3:4, 9:16) — arbitrary pixel dimensions aren't exposed, matching what the underlying model actually supports; this isn't a code gap, it's an inherent constraint of the AI model itself.

### 6.3 Ninja Legal `[BE+FE]` ✅ complete (scope disclosed below)

**Audited before building** (backend `services/ai-legal` + full frontend `ninja-legal`, same method as 6.1/6.2): the "Contract Generation" page itself was NOT actually empty/broken — a full chat-guided generation flow (create contract → AI "Contract Coach" chat → 5-section AI generation → hover-to-edit → PDF export) was already wired correctly end-to-end, confirmed by direct code read and live testing. But the audit found real, concrete problems elsewhere: a live bug that stringified contract parties as `[object Object]` in the generation prompt, no disclaimer anywhere in the actual generation path or export, a plain "gpt-4" model, dead code (two unused controller methods, a whole unused OpenAI function, three dead React components with a triplicated "update section" UI), a validation bug in the contract-creation form that made a natural minimal fill-in 400, and — the most serious find — two entire pages (`AllContracts.tsx`, `AuditLogs.tsx`) that were 100% hardcoded fake data, including fabricated "GDPR Compliance Scan: Passed" / "SOC2 Control Check: Passed" entries that could mislead a founder into thinking real compliance scans had run.

- [x] **Contract Generation page — verified working, real bugs fixed.** Root-caused and fixed the `contract.parties?.join(", ")` bug (stringified party *objects*, not names) in the live generation/section-update prompts (`services/ai-legal/src/services/openaiService.js`). Upgraded the model from "gpt-4" to "gpt-4o" across every AI call in the service. Deleted confirmed-dead code: two unused `AiLegalController` methods (`callAIForContractChat`/`callAIForContractGeneration`), the unused `generateCompleteContract` function they called, and three dead frontend components (`GeneratedContractModal.tsx`, `SectionUpdateModal.tsx`, `NoContracts.tsx` — zero importers anywhere, one of which contained its own inline triplicate of the other). Verified live end-to-end with a real OpenAI call: correct party names, correct NDA-specific sections, disclaimer section present.
- [x] **"Workforce Manager" copy fixed** — `NinjaLegalHeader.tsx` now reads "Your AI Legal Assistant — Draft, Review, and Compare Contracts with Confidence" instead of Ninja Ops' leftover copy.
- [x] **Disclaimer + professional-review recommendation — added everywhere it was missing.** New shared `LegalDisclaimerBanner.tsx`, added to the main dashboard, the Contract Generation preview (both on-screen and baked into the exported PDF itself, not just the UI around it), the new Analysis page, and the new Comparison page. The AI generation prompt now also requires a mandatory "Disclaimer" section on every generated document, with a guardrail so refinement requests can reword it but can't strip its substance (verified live: asked it to shorten the disclaimer, it complied while keeping every required element).
- [x] **Document-type generation — built for real, reusing the existing AI drafting engine rather than a parallel template system.** `Contract` gained a `documentType` field (NDA, Service/Freelancer/Employment/Contractor/Partnership/Consulting Agreement, Terms of Service, Privacy Policy, Refund Policy, or General) with a new `documentTypes.js` guidance map that steers the generation prompt's structure per type (e.g. an NDA gets confidentiality/exclusions/remedies sections, a Privacy Policy gets a one-sided-policy framing instead of a two-party signature block). New "Document Type" picker in `AddContractModal.tsx`. Verified live: an NDA-typed contract generated genuinely NDA-shaped sections, not a generic contract.
- [x] **Contract upload/analysis — built for real.** New `UploadedContract` model, S3-backed upload (PDF/DOCX/TXT via `pdf-parse`/`mammoth`), and a real OpenAI analysis call returning a plain-English summary, obligations, payment terms, termination clauses, and risky clauses (each with a severity level and a plain-English explanation of why it matters) — deliberately NOT legal advice, framed as assistance throughout. New `/ai-tools/legal/analyze` page: upload, view analysis, retry on failure, delete, export the analysis as a PDF. Verified live with a deliberately one-sided test contract (uncapped liability, no-refunds, one-sided termination) — the analysis correctly flagged all three as high/medium-severity risks with accurate explanations.
- [x] **Contract comparison + export — built for real.** New `POST /compare-contracts` endpoint compares two uploaded documents' actual text via OpenAI and returns a structured key-differences table, a favorability read, and a non-legal-advice recommendation. New `/ai-tools/legal/compare` page. Verified live with two deliberately-different test contracts (payment terms, termination, liability, IP timing all varied) — the comparison correctly identified every real difference and correctly judged which version was more favorable to which side. "Export" — PDF download already existed for generated contracts; the same pattern was extended to the new Analysis page.
- [x] **Two fabricated-data pages found and fixed** (not on the original checklist, but the same "don't fabricate data" standard applied elsewhere this cycle): `AllContracts.tsx` was 100% hardcoded fake companies with non-functional buttons — rewired to the real `listContracts` API with working status filters, search, pagination, and a working contract-detail view. `AuditLogs.tsx` fabricated "Passed" results for compliance scans that don't exist — first replaced with an honest empty state, then (see below) with a genuinely real scanning feature.
- [x] **Real validation bug fixed.** `AddContractModal.tsx`'s client-side validation only checked Contract Title, but the backend requires `priority`/`contractStatus`/`expiryDate` too — filling in only the visibly-required fields (a natural first attempt) produced a confusing 400. Fixed with real, sensible defaults (priority: medium, status: active, expiry: one year out) plus a proper asterisk + validation on expiry date as a backstop.
- [x] **Delete contract — built for real, with cascade.** New `DELETE /ai-legal/contracts/:contractId` deletes the contract plus every AI-generated version and its chat history (nothing orphaned, same cascade principle used for image versions in 6.2). New Delete button + confirmation dialog in `ViewContractModal.tsx`, wired into every page that opens it (dashboard, All Contracts). Verified live end-to-end: created, deleted, confirmed gone from both the API and the UI list.
- [x] **Compliance scan — built for real, scoped honestly.** Explicitly scoped with the user first: literal infrastructure/SOC2-style scanning would need OAuth into real cloud/HR systems this product has no integration with, and faking that would recreate the exact fabrication problem just removed. Built the achievable, honest version instead — reuses the same real document-analysis engine as Contract Analysis: a user's uploaded document is checked against a defined GDPR/security-policy/HIPAA *wording* checklist (lawful basis, data subject rights, retention, breach notification, confidentiality, encryption commitments, incident response, access control, PHI/BAA language when relevant) and returns a per-area Covered/Partial/Missing/N-A verdict with findings and recommendations, plus an overall readiness level. The UI and prompt both explicitly say this reviews document text, not real infrastructure. `AuditLogs.tsx` rebuilt around this: pick or upload a document, run a scan, browse real scan history, re-run. A real bug was caught and fixed during verification: the `recommendation` field is deliberately empty for "covered"/"not_applicable" areas, but the schema had it `required`, so every real scan 500'd on save — fixed, and the save path made defensive so a failure always lands the scan in a terminal state instead of stuck at "pending" forever. Verified live with a deliberately weak test privacy policy — correctly flagged missing lawful-basis, data-subject-rights, and breach-notification language as gaps, and correctly marked HIPAA items not-applicable since no health data was involved. Both new pages (All Contracts, Compliance Scan) were previously unreachable from any navigation — added to the sidebar so they're actually usable.

### 6.4 Onboarding, Settings, Analytics, Admin `[BE+FE]` ✅ complete (scope disclosed below)

**Audited before building** (same method as 6.1–6.3, across `auth-service`, `admin-service`, `social-media-service`, and every frontend admin/settings/team surface): found the same "looks real but isn't" pattern recurring in the Admin Dashboard specifically — a deterministic-fake `getSubscription()` plan generator, a no-op "Edit User" save button, 10/11 services hardcoded to `"healthy"` regardless of reality, `Math.random()`-driven usage charts, and a `getAnalytics` endpoint that only ever read from an `Analytics` collection nothing had ever written to (confirmed: always returned `[]`). Also found a genuinely useful pre-existing asset nothing was using yet: `UserActivity`, a real, durable (non-TTL) login/logout event log already being written on every login — unlike `LoginSession` (which auto-expires and can't support historical analytics), this could directly power real DAU/MAU/retention once wired up.

- [x] **Onboarding — built for real, backed by a new shared `BusinessProfile` model.** New `BusinessProfile` (auth-service): businessName/industry/description/targetCustomer/website/socialAccounts/products/brandTone/goals, one profile per account, plus `completedOnboarding`/`skippedOnboarding` flags. New 3-step onboarding wizard (`Onboarding.tsx`) gates every real (non-admin, non-team-sub-user) login via a new `BusinessProfileProvider` + `ProtectedRoute` redirect — skippable, revisitable later from Settings. Reused by all four AI-generation surfaces: Ninja Chat (mirrors the existing `memoryFacts` system-prompt injection pattern), Website Builder's AI section editor, Social Pro's post generation (merged with — not duplicating — the existing narrower `BrandVoice`, which wins per-field when set), and Image Generation (folded into the existing brand-assets guidance channel, gated behind the same "apply my brand" opt-in so it can't bias unrelated prompts). Verified live end-to-end: asked Ninja Chat "what business am I running" with zero context in the message itself, got back the correct saved business name and description; a real Social Pro post generation for a saved aerospace-rocket business came back with correctly on-topic hashtags/copy without ever being told the business explicitly.
- [x] **Settings — completed for real.** Dead `ProfileFormState` fields (company/jobTitle/location/bio — declared, rendered nowhere, zero backend support) deleted rather than wired to nothing. New "Business info" card (same `BusinessProfile` model, editable here too). New "Connections & data" card: real links to where Connected Accounts (Social Pro), Connected Domains (Web Builder), and Memory (Chat) already live — previously reachable only by drilling into those features directly, now discoverable from Settings. New real account-data export (`GET /user/export-data`): profile, preferences, business profile, subscription, and transaction history as a downloaded JSON file — scoped to this service's own data, not a claim of exporting every piece of content across every microservice. Password/security, billing/plan, and language/region were already real and untouched.
- [x] **Real IANA timezones — audited, three real bugs found and fixed.** `Settings.tsx`'s timezone default was already fixed in an earlier pass; this audit found three more: (1) `PostsTable.tsx` rendered scheduled/published post times with a naive `toLocaleDateString()`/`toLocaleTimeString()` (browser-local time, ignoring the saved preference) — fixed to render in the viewer's actual saved IANA timezone. (2) `AllContracts.tsx`'s contract expiry date rendered via naive local-time `toLocaleDateString()` on a UTC-midnight-stored calendar date — a real off-by-one-day bug for any timezone west of UTC (Dec 31 displaying as Dec 30) — fixed to render in UTC, correctly treating it as a timezone-invariant calendar date. (3) **The real one**: Social Pro's scheduler already computed a correct UTC instant client-side from the user's IANA timezone (`buildUTCFromTimezone`) and sent it to the backend — but the backend silently discarded it and recomputed the scheduled time from the raw date/time fields using the **server's own local timezone** (`new Date(y, m-1, d, hh, mm)`), so any user not in the server's timezone got their post scheduled at the wrong real-world instant despite the frontend doing everything right. Fixed by porting the same `Intl.DateTimeFormat`-based conversion algorithm to the backend and trusting the frontend's precomputed value when present. Verified live: a 9am Asia/Karachi schedule request round-tripped through the real endpoint and came back as the exact correct UTC instant (`2026-12-01T04:00:00.000Z`), matching a standalone unit check of the new conversion function against known timezone offsets.
- [x] **Team Management — real invitation system, real empty state, real pagination, usage surfacing.** Previously: adding a member meant the owner typed a password *for* them directly, with zero email, zero pending state, and permanently-disabled Previous/Next buttons. Now: adding a member offers a real choice — "Send email invite" (default) generates a signed, hashed, 7-day-expiring token, emails a real accept link, and the member sets their own password — or "Set password now" for the old direct-set flow. New public (no-auth) accept-invitation endpoints + a new `/accept-invite` page. Members mid-invitation show a real amber "Pending invite" badge with a working "Resend invitation" button. A genuine first-run empty state ("No team members yet" + an inline "Add your first member" CTA) now shows only when the team is actually empty, distinct from the existing "no results for this filter" state. Pagination is now real client-side paging (10/page) instead of two permanently-`disabled` buttons. Team-member usage/limit (`X / Y team members used`) now surfaces directly on the page instead of being invisible until a rejected Add-Member call. **A real bug was caught during live Playwright verification and fixed**: the new "Resend invitation" endpoint threw an unhandled 500 (with a misleading generic "Server error" toast) whenever the email itself failed to send, even though the token had already been regenerated successfully — `addMember`'s existing soft-fail pattern for the same failure mode wasn't mirrored in `resendInvitation`. Fixed to soft-fail the same way and surface an honest, specific message either way. Verified live end-to-end: full invite → accept → real login cycle confirmed working (including the account genuinely being unable to log in before acceptance), plus the resend-failure fix confirmed against the exact scenario that broke it.
- [x] **Product Analytics — built for real**, replacing the dead `getAnalytics`/`Analytics` collection outright (deleted, not left as unreachable dead code) with a new `analyticsService.js` computing everything from real data: daily signups (`User.createdAt`), real DAU/MAU (from `UserActivity`'s durable login log — a real bug was caught and fixed here too: the first implementation guessed a separate `startup_ninja_auth` database existed for this data, based on a pre-existing but effectively-vestigial cross-database helper elsewhere in the codebase; direct inspection confirmed every service in this deployment actually shares one database, and the fix was verified live against the real login activity generated during this session itself), weekly signup-cohort Day-7/Day-30 retention, subscription churn, trial-to-paid conversion (defined against real completed-transaction history, not a "trial" concept this app's billing doesn't actually have), MRR/ARR/ARPU (from real per-subscription `amount`, correctly normalizing annual billing to monthly), and a refunds list (real query against `Transaction`, honestly empty today since no refund-issuing code path exists yet anywhere in the app — a true empty state, not a fabricated one). New `/admin-dashboard/analytics` page reusing the app's existing hand-rolled SVG chart components (no new charting library added). Verified live via real triggered activity and a direct unit check of the timezone-sensitive parts of the pipeline.
- [x] **Admin Dashboard: real error tracking, scoped as a lightweight capture, not a full APM/Sentry replacement.** New `ErrorLog` model + internal endpoint, populated by hooking directly into the API gateway's shared proxy layer (both the common `utils/proxy.js` helper used by 7 of the 8 gateway route files, and the one standalone catch-all proxy in `routes/admin.js` that doesn't share it) — every request to every backend service already flows through one of these two, so this needed no per-service instrumentation for broad, real coverage. New "Recent Errors" panel on the main admin dashboard (service breakdown + a live recent-error feed). Verified live by deliberately triggering a real 500 (an invalid ObjectId cast) through the gateway and confirming it appeared correctly in the panel's data within a second, with the right service, route, method, and error message.
- [x] **Admin Dashboard: real support ticket system, scoped as a simple internal ticketing flow, not a full helpdesk** (no SLAs, no routing rules, no canned responses). New `SupportTicket` model (subject/message/status/priority/reply thread). User-facing: a new `/support` page (submit a ticket, see your own tickets, open a thread and reply). Admin-facing: a new `/admin-dashboard/support` page (every ticket across every user, status filters, reply, change status — a first admin reply automatically moves a fresh ticket to "in progress"; a user replying to a "resolved" ticket automatically reopens it). **A real bug was caught during live Playwright verification and fixed**: changing a ticket's status (and, compounding it, replying afterward) returned the ticket without its `userId` populated — unlike the list/get endpoints — so the admin modal's reporter email flipped to "Unknown user" right after a status change until the next full page reload. Fixed by populating `userId` in both the status-update and admin-reply responses, matching the read endpoints; re-verified live that the response now carries the real user object. Full lifecycle verified live end-to-end: user submits → admin sees it with the correct user/priority/status → admin replies and resolves (email now stays correct throughout) → user sees the updated status and the admin's reply distinguished from their own in the thread → filtering by "Resolved" correctly surfaces it.
- [x] **The dead `Analytics` model repurposed away entirely** — deleted outright once nothing referenced it, rather than left as unreachable dead code alongside the new real analytics engine.

**Re-audited and the three remaining gaps above have since been closed for real** (independent re-audit agent verified every other claim in this section against actual code — all held up — then flagged these three as genuinely open; all three are now built and live-verified, not just claimed):

- [x] **Account-deletion cascade — built for real, across all 9 backend services.** Every one of the other 8 microservices (ai-legal, imaginative-service, social-media-service, website-builder-service, ai-chat, chatbot, ninja-sales-service, admin-service) now exposes a real `DELETE /internal/account-data/:userId` (service-to-service only, confirmed via direct grep that no gateway route anywhere exposes an `/internal` path) that deletes every document that user owns in that service plus their S3-stored files, reusing each service's own existing delete/S3-cleanup patterns where one already existed (e.g. `website-builder-service`'s `deleteWebsite`, `imaginative-service`'s `deleteImage`). auth-service gained a matching cleanup for its own extra data (cards — including a real Stripe payment-method detach, login sessions, business profile, student verifications + S3, activity history), deliberately leaving `Subscription`/`Transaction` untouched (financial/audit records, not "content"). A new `accountDeletionCascade.js` orchestrator fires all 8 service calls in parallel via `Promise.allSettled` so one slow/down service can't block or fail the others, and is wired into **both** deletion paths — self-service `deleteAccount` and, critically, a **second real bug found during this re-pass**: the admin-facing "Delete User" button (`adminController.js`'s `deleteUser`) was doing a raw hard `findByIdAndDelete` with a `// TODO: delete content in other services` comment, no anonymization, and — in a real separate-database production topology — wouldn't even have removed the login-capable auth-service copy. Replaced with a call into the same real cascade logic (a new admin-authorized `DELETE /user/admin/users/:userId` on auth-service), so both deletion paths now behave identically and correctly. **Verified live end-to-end**: seeded a real test account with real documents in `ai-legal`, `social-media-service`, `admin-service` (SupportTicket), and a `BusinessProfile`, triggered the real admin-delete endpoint through the full gateway → auth-service → 8-service cascade chain, and confirmed every one of those documents was actually gone afterward and the user record was properly anonymized (`status: 0`, email replaced) — not just that the HTTP calls returned 200.
- [x] **Refund issuing — built for real, live-tested against real Stripe test-mode money.** New `POST /user/admin/transactions/:transactionId/refund` (auth-service, admin-only) calls Stripe's real `refunds.create` against the transaction's actual `stripePaymentIntentId`/`stripeChargeId`, records it as a new linked `Transaction` (`transactionType: 'refund'`), and guards against the real failure modes found while building it: refunding a transaction with no real Stripe charge on file (e.g. an admin-assigned subscription with a placeholder ID) is rejected with a clear message instead of crashing or hitting the wrong Stripe object, and double-refunding the same transaction is blocked (confirmed live — Stripe's own idempotency caught it even before my own pre-check, so the dangerous outcome — refunding twice — is defended in depth). New "Refund" button in the admin user-detail Subscription tab's transaction table. **Verified live with real Stripe test-mode API calls**: created a real test PaymentIntent, issued a real refund through the actual endpoint (got back a genuine `re_...` Stripe refund ID), confirmed the double-refund guard, and confirmed the new refund transaction immediately appeared in the Product Analytics refunds view.
- [x] **Feature usage, adoption, activation, and paid-user counts — added to Product Analytics**, closing a gap the re-audit found against `feedback.md`'s explicit wording ("activated users, feature usage, ... paid users, ... adoption") that the original 6.4 pass had missed. Three new `analyticsService.js` functions, all from real data: per-feature call counts + distinct-user "adoption %" from `APIUsageLog` (the same real usage-reporting data every AI feature already writes to), a real activation rate (% of a signup cohort who made at least one real feature call within 7 days — the standard SaaS activation definition), and a real paid-vs-total user count from `Subscription`. New KPI cards and a feature-adoption bar chart on the Analytics page. Verified live with real data from this session's own usage (Ninja Chat/Image Generation/Social Pro all showing real call counts and 100% adoption among the one active test account).

---

## Phase 7 — Security & Public Pages `[BE+FE]` ✅ complete (scope disclosed below)
(`feedback.md` §11 — run in parallel with Phases 1-6, not deferred to the end)

**Audited before building** (dedicated parallel audits for secret hygiene, file-upload handling, and authorization/ownership checks, on top of direct manual review of rate limiting and CSRF — same method as every prior phase): found the app's security posture was a genuine mix — some real, already-correct defenses (helmet CSP, NoSQL-injection sanitization, per-route rate limiters, most controllers correctly scoping queries to `req.user`) alongside several real, concrete gaps, the most serious being a systemic authorization bug in `website-builder-service` and CSRF protection that had been fully disabled with a hardcoded dummy token.

- [x] **Automated database backups — built for real, verified against real S3 uploads.** New `databaseBackup.js` (admin-service): prefers the real `mongodump` tool when installed, falls back to a native-driver per-collection export when it isn't (confirmed this dev machine doesn't have it installed — the fallback path is what actually ran, not a hypothetical), uploads to S3 under a dated prefix, prunes backups older than 30 days. Runs automatically on a daily in-process interval (mirroring the existing `usageSyncService` pattern) plus once ~60s after boot, and is also manually triggerable by an admin. A failed backup is logged into the same `ErrorLog` the admin dashboard already surfaces — a broken schedule is visible, not a silent gap. **Verified live**: triggered a real backup, confirmed 44 real collection files landed in S3 with real content (not empty stubs), confirmed the automatic boot-time run had also already fired successfully on its own.
- [x] **Error monitoring** — already built in Phase 6.4 (gateway-level `ErrorLog` capture + admin dashboard panel); no additional work needed here, just confirmed still healthy.
- [x] **Secret management audit — clean, no real findings.** Confirmed `.env`/`evns/.env*` are properly gitignored and were never committed in either repo's git history (checked directly, not assumed). Confirmed no backend endpoint leaks `process.env` or exposes a debug/config route. Confirmed the frontend's `VITE_*` values that looked sensitive at a glance (`VITE_GOOGLE_FONTS_API_KEY`, `VITE_WEBSITE_BUILDER_LICENSE_KEY`) are vendor-intended for client-side use (Google Fonts API keys and GrapesJS Studio SDK license keys are both designed to ship in the browser bundle, confirmed by reading how each is actually used) — not accidental secret leaks. Confirmed `User.password` has `select: false` at the schema level as defense-in-depth on top of explicit query exclusions already used throughout.
- [x] **Rate limiting — extended real coverage to the most cost-sensitive routes.** The gateway already had real, working tiered rate limiting (strict auth limiter, AI limiter, general limiter, all keyed by user-or-IP) — but the stricter AI limiter only matched `/api/ai-content`, so Image Generation and Ninja Legal's contract drafting (the two priciest real per-call operations, with actual $ cost per request) were falling through to the much more permissive general limiter. Extended the AI limiter to cover every fully-AI service (`imaginative`, `ai-legal`, `chatbot`) plus the AI-specific sub-paths of the mixed CRUD+AI services (`social-media`'s and `website-builder`'s `/ai/...` routes), leaving their regular CRUD traffic on the general limiter.
- [x] **Authorization checks — one critical, systemic bug found and fixed, plus a real stale-permission gap.**
  - **`website-builder-service`: every website-scoped endpoint trusted a client-supplied `userId`** (from `req.query`/`req.body`) as the ownership filter instead of the authenticated caller — meaning the "ownership check" (`findOne({_id, userId})`) always matched, since the attacker controlled both sides of the comparison. Confirmed genuinely exploitable, not theoretical: the real frontend already sends `userId` as a plain request field on every one of these calls, and a live test confirmed a non-owner could read another user's live website data by simply supplying the victim's real user id — even their own admin account, with no special owner permission, could reach it. Fixed across all 21 call sites (create, delete, list, read/save website data, publish, connect/verify/disconnect domain, auto-configure DNS, update SEO/social links, upload favicon, get/create/update/delete pages, list/restore versions, AI section editing) to derive `userId` only from the authenticated JWT. One of these (`analytics`) had gone further — a `findById` with **no** ownership filter at all, plus a dead-code block that computed a mismatch condition and never acted on it — now genuinely enforced (with an admin bypass, deliberately preserved). **Verified live**: the exact same attack that used to succeed (fetching another real user's website data by supplying their real user id) now correctly returns 404, while the legitimate owner's own access still works.
  - **Stale-JWT-role trust, in both `auth-service` and `admin-service`.** A JWT is signed once at login and never re-issued mid-session — both services' admin-role checks previously trusted the role claim baked into that token, with no re-check against the database. If an admin's role were later revoked, their existing token would keep full admin access (including, in auth-service's case, viewing other users' uploaded ID verification images) until the token naturally expired. `auth-service`'s `requireRole` and `admin-service`'s `adminAuth` both now re-verify against the current database role on every check (failing closed on any DB error, rather than the three JWT-trusting fallback paths `adminAuth` previously had). **Verified live**: legitimate admin access still works normally across both services, and a non-admin account is still correctly rejected.
  - Removed a low-severity dead-code fallback in `imaginative-service` (`req.query.userId`) that was unreachable today but a latent trap if that handler were ever reused behind optional-auth middleware later.
- [x] **Secure file uploads — one critical stored-content-confusion bug fixed, plus a systemic path-traversal issue and several missing safeguards.**
  - **`website-builder-service`'s document uploads**: the file extension actually written to disk came from the client-supplied filename, never cross-checked against the mimetype that had already been validated (both by `fileFilter` and a real magic-byte check) — so real image/PDF bytes could be stored under an attacker-chosen extension like `.html`. Compounding it, the entire `uploads/` tree — including these documents — was served publicly and unauthenticated via `express.static`, completely bypassing the real JWT+ownership-gated download endpoint that was supposed to be the only way to reach them, and letting the browser infer a Content-Type from that same attacker-influenceable extension. Fixed: the stored extension is now derived from a fixed mimetype→extension table, never the client filename; `/uploads/documents` is blocked from static serving entirely (the authenticated download route is the only path to it now); `image/svg+xml` (a real XSS vector for user uploads, and one that never actually validated against this endpoint's own magic-byte check anyway) removed from the allowlist.
  - **`social-media-service`: unsanitized filenames in temp file paths, at all 5 places media gets uploaded** (the scheduler and all four platform posting controllers) — a crafted `../../../whatever` filename could write outside the intended temp directory. Fixed everywhere with `path.basename()`. Also added a missing `fileFilter` (previously accepted any file type) to the scheduler's own upload config.
  - Added a missing `fileFilter` to `ai-chat`'s attachment uploads (previously accepted any file type; now matches the same allowlist the app's own document-text-extraction logic already treats as "supported") and to `ninja-sales-service`'s invoice/proposal logo uploads (defense-in-depth — the real validation was already happening downstream before storage, this just stops non-image files from being buffered into memory at all).
  - **API gateway: three of its own `multer()` re-parsing layers had no size limit at all** (`ai-legal`, `imaginative`, `social-media` proxy routes) — meaning the gateway itself, not the (correctly-capped) downstream service, was the actual memory-exhaustion weak link, regardless of how strict each service's own limit was. Capped each to match (or safely exceed) its downstream service's real limit.
- [x] **CSRF protection — properly re-enabled, and a critical regression it exposed was found and fixed the same day.** CSRF middleware existed and was fully built (the `csrf-csrf` library, real token generation, a real skip-list for public routes) but had been left commented out with a hardcoded dummy token that validated against nothing — the frontend was faithfully fetching and attaching a token that the backend never actually checked. Re-enabled it for real: fixed the skip-list's paths (missing the `/api` prefix that every real incoming request actually has, which would have broken login/register once protection was live), added the Stripe webhook to the exemption list (an external caller that can't supply a CSRF token), and matched the error message casing to what the frontend's own retry-on-failure logic already checks for. Along the way, fixed the gateway's global error handler, which was flattening every thrown error — including the CSRF library's own 403 — into a generic 500, which would have silently defeated that same frontend retry logic. **A live browser verification pass then caught a critical regression before it reached anyone**: in this dev environment the frontend (`https://localhost:3000`) and the gateway (`http://localhost:5000`) differ in scheme, which Chrome's Schemeful-SameSite policy treats as cross-site — silently refusing to store the CSRF cookie at all, which broke every single POST/PUT/DELETE in the entire app. Root cause: the frontend's API base URL was an absolute cross-origin address, even though a same-origin Vite dev-server proxy for `/api` already existed and was simply never being used. Fixed by pointing the frontend at a relative `/api` path instead, so browser requests stay same-origin and Vite transparently proxies them — eliminating the cross-origin request (and the bug) entirely, with no change to production behavior. **Re-verified live end-to-end** across five different real flows (Settings save, Team add/delete, Ninja Chat, Support ticket) with zero CSRF failures.
- [x] **Account/data deletion tested end-to-end** — this is the same real cross-service cascade-delete work from the Phase 6.4 re-audit (see above): real content removal verified live across `ai-legal`, `social-media-service`, `admin-service`, and auth-service's own extra data, through both the self-service and admin-initiated deletion paths.
- [x] **Public pages — audited, then built/fixed for real.** `Terms`, `Privacy`, `Contact` (a real public form, already wired to a real backend endpoint), and `Pricing` (real live plan data, not hardcoded) already existed; Terms and Privacy existed but were generic boilerplate with no mention of this app's actual AI features, subscriptions, or data flows — enriched both with real, specific content (AI-generated-content ownership and its limits, subscription/cancellation terms, and — for Privacy — Stripe, Google/Microsoft OAuth, third-party AI processing of user content, and the real data-export/account-deletion behavior built earlier in this phase). **Refund Policy, About, and FAQ pages were completely missing** — built all three for real (Refund Policy describes actual billing behavior — no partial-period refunds, but real ones for billing errors; About covers the real product suite; FAQ covers real, non-generic questions about credits, plans, data export/deletion, and AI content ownership). Footer was missing links to Privacy Policy (existed but was never linked from anywhere in the footer), Refund Policy, About, and FAQ — added all four; also fixed a dead `link: "#"` FAQ card on the Resources page to point at the new page.
- [x] **Cookie consent — audited, genuinely not required, disclosed rather than silently skipped.** No cookie-consent banner exists, and none was built. Confirmed why this is currently fine rather than assuming it: this app sets no non-essential/tracking cookies and loads no analytics or ad-tracking scripts anywhere (`index.html` and the whole `src/` tree only reference Google Fonts and Stripe.js, both treated as strictly necessary, not tracking, under the regulations this kind of banner exists for). **This should be revisited the moment any analytics/tracking script is added** — noting it here so that's a deliberate future decision, not a forgotten one.

- [x] **Follow-up: `admin-service`'s local `User` model now excludes `password`/`inviteTokenHash`/`inviteTokenExpires` by default** (`select: false`, matching auth-service's real schema exactly), closing the lower-severity defense-in-depth gap noted below. Tracing every read path before applying it caught a real bug it would otherwise have silently introduced: `acceptInvite`'s expiration check (`member.inviteTokenExpires.getTime() < Date.now()`) fetched `member` with no explicit `.select()`, so it would have silently stopped enforcing invite-link expiration entirely once the field was schema-excluded — fixed by adding `.select('+inviteTokenExpires')` to that one query. Every other read/write path (team listing's existing exclusion-select, resend-invitation and reset-password's overwrite-then-save patterns, fresh-document construction) was confirmed safe by the same trace. Verified the team-members endpoint still returns `success: true` after the change.
- [x] **Follow-up: `/documentation` built for real**, closing a previously-disclosed gap (this page was genuinely unbuilt, with its footer link left commented out, since it wasn't part of this phase's original explicit checklist). Real content for all 8 real modules, grounded in actually reading each feature's own frontend/backend — not guessed or templated — footer link restored, zero dead "#" links, verified live.

**Disclosed remaining scope**: `services/ninja-sales-service`'s team-scoping (`isMemberRestrictedToAssignedLeads`) is a real, intentional, already-correct design — confirmed, not a gap, no action needed.

---

## Phase 8 — Final Release QA & Launch Positioning `[BE+FE]` ✅ complete (scope disclosed below)
(`feedback.md` §12 + Final Launch Positioning Check — do this last, once Phases 0-7 are complete)

**Audited before building**: ran an independent, skeptical re-check of `feedback.md` §1-7 against actual code (not trusting existing plan.md checkmarks), plus a dedicated audit of every public-facing landing page for ChatGPT/Claude-competitor framing, overclaiming vs. real built features, unproven social-proof numbers, and missing "Coming Soon" labeling. Then ran five parallel live-browser (Playwright/Chromium) regression passes across the full account/billing lifecycle, Chat/Image Gen/Web Builder, and Social Pro/Sales/Legal, plus two dedicated sweeps for the "Invalid Date" P0 item. Every real finding below was fixed and re-verified, not just logged.

- [x] **P0 completeness re-check (`feedback.md` §1-6)**: confirmed done with code evidence — silent session refresh (24h token, not a 10-15min timeout), plan structure (Go/Go Student/Pro/Business/Custom, no stale Founder/Growth/Free labels), student verification + image deletion, server-side quota enforcement, Ninja Chat search/citations/file validation, Web Builder wizard + 20 templates (floor of the requested 20-30 range — not expanded further, a content-volume nice-to-have rather than a defect), Ninja Sales soft-delete/restore/cascade. One genuinely open item, disclosed rather than hidden: **domain/SSL provisioning (Phase 4) cannot be live-verified end-to-end in this dev environment** (no public IP/real DNS/ACME-reachable server) — did a full manual trace instead of a live test, across `domainSslService.js`, `certbot/provision-domains.sh`, `nginx-proxy/nginx.conf`, and `docker-compose.prod.yml`'s volume/container wiring (shared `ssl-queue`/`custom-domains` mounts, `docker.sock` + `docker-cli` for the reload step, the catch-all HTTP:80 ACME-challenge server block that solves the chicken-and-egg cert-issuance problem) — internally consistent, no logic bugs found, but genuinely unverified against a real Let's Encrypt handshake. Do not market "publish in minutes" as proven until someone runs it against a real domain. Minor, non-blocking spec deviation also noted: Ninja Sales pipeline has 9 stages (incl. an extra "Converted" stage) vs. feedback.md's 6-stage list — functionally fine, a product-naming decision rather than a bug.
- [x] **"Remove every 'Invalid Date' record" (P0)** — genuinely closed, not just spot-checked: fixed the shared `src/utils/date.ts` helpers (guarded against a truthy-but-unparseable date string, not just an empty one), then two full sweeps found and fixed 15 named local `formatDate`/`getRelativeTime`-style functions (BillingHistory, UserManagementTable, PostsTable — two of which used `Intl.DateTimeFormat().format()`, which *throws* on an invalid Date rather than just rendering text, a real crash risk not just a cosmetic one — plus admin/ninja-sales/ninja-legal/social-media/dashboard files) and 39 more files with raw inline `{new Date(x).toLocaleDateString()}`-style JSX expressions that had no guard, or only a truthy-check that a malformed-but-non-empty string would still slip past. `npx tsc --noEmit` clean after both passes.
- [x] **Full regression pass, live browser (Chromium/Playwright), fresh + existing account**: fresh signup → real OTP-verification UI; existing-account login/session/logout/password-reset all clean; Settings full sweep (all 9 sections render real data, one real save verified with toast); Ninja Chat (search+citations, file attach+reference, history search, delete); Image Gen (generate+download+delete); Web Builder (open/select/AI-edit/save/preview/domain-settings); Social Pro (AI-write + schedule + calendar); Ninja Sales (create/edit/persist-after-reload/delete/restore, with the honest finding that Leads use a table+status-dropdown, not a kanban — the drag-and-drop kanban is a separate "Projects Pipeline"/Deals entity); Ninja Legal (generate NDA + disclaimer banner + contract analysis + PDF export). CSRF cross-checked on every write action across all of the above — zero CSRF failures, zero false-positive 404s on a user's own content, confirming the Phase 7 authorization/CSRF fixes didn't break legitimate access.
- [x] **Two real bugs found and fixed during regression, not just documented**: (1) `seed-demo.js` (the premium-demo-account seeder) hardcoded every usage limit to a flat `15000` and set a 10-years-out `periodEnd`, instead of the account's real Business-plan limits and a normal 30-day billing cycle — this is exactly the "obviously wrong renewal date" class of bug feedback.md's P0 item warns about, just in the future direction. Fixed to derive limits from the canonical `services/auth-service/seed-plans.js` source (single source of truth, same file Phase 6.4 already established) and a real 30-day period; re-ran the seeder against the live DB and confirmed via a real login call that `/user/subscription` now returns correct limits (e.g. 4,000 AI chat messages, not 15,000) and a `nextBillingDate` 30 days out, not a decade. (2) Settings page overflowed horizontally on mobile (390px) — every card section measured 432px because the responsive grid wrapper in `Settings.tsx` only set `grid-template-columns` at the `lg:` breakpoint, so below that it fell back to the browser's default implicit column with no `minmax(0,1fr)` safety net. Fixed with one line (`grid-cols-1` added to the base classes); re-verified live at 390px/810px/1440px — zero overflow at any width, desktop two-column layout unchanged.
- [x] **One finding investigated and knowingly left as-is, disclosed rather than silently patched over**: Website Builder's "Ask Ninja" AI edit can 500 when nothing is selected (whole-page edit mode) on a sufficiently complex page. Traced the full message-forwarding chain (backend controller → gateway proxy → frontend service → toast) and confirmed it already correctly surfaces a specific, actionable error message end-to-end — this isn't a swallowed-error bug. The actual cause is a known, already-disclosed LLM-reliability limit (GPT-4.1-mini doesn't always echo back 100% of a large multi-section page verbatim when asked for a small edit), which a prior session already added defense-in-depth checks for specifically to fail loudly with a helpful message ("try narrowing your request to a specific section") rather than silently drop content — and the UI already warns proactively before submission ("Editing the whole page — select something in the canvas to target just one section"). Select-then-edit (the primary, recommended workflow) works perfectly in every test run.
- [x] **Positioning check — real violations found and fixed, not just reviewed**: removed named-competitor comparisons (ChatGPT Plus line-items in `PricingMain.tsx`/`SolutionsMain.tsx`'s cost-comparison tables, relabeled to generic tool categories, dollar totals recalculated to match the real Pro-plan price of $39/mo rather than a stale, non-existent "$29/mo"); removed a fabricated "Success Stories" section in `BusinessMain.tsx` that was rendering invented stats ("10x", "5+", "$50k+") under a code comment literally reading `{/* Case Study Placeholder */}`; removed three fabricated "Latest News" items implying an undisclosed OpenAI partnership, a fake "1 Million Generated Projects" milestone, and an unbuilt "real-time collaboration" feature, replacing them with real feature-accurate items; fixed Ninja Legal's marketed feature list (was claiming unbuilt "Founder Structuring"/"Equity Modelling"/vague "Compliance Roadmap" — replaced with the four features that actually exist: Contract Generation, Upload & Analyze, Contract Comparison, Compliance Scan); removed an unfootnoted "AI Chat (Unlimited*)" claim that directly contradicted the FAQ's own "usage resets each billing cycle" language; removed two "thousands of founders" unproven-scale claims; labeled two unverified Business-plan claims ("White-label options", "Custom integrations") as Coming Soon; fixed an "across all platforms" social-analytics overclaim (real engagement analytics are Facebook/Instagram-only) to generic "connected accounts" wording; deleted a stale, unreachable duplicate `/terms` page (`TermsMain.tsx` + its dead dispatcher mapping) that a static App.tsx route already fully shadowed, left over from before the real `Legal/TermsOfService.tsx` was written. `npx tsc --noEmit` clean throughout.
- [x] **Cross-browser/device disclosure**: every regression pass ran on Chromium only — this Windows dev environment has no real Safari/WebKit, and Edge is the same Chromium engine under a different label so running it separately would add no genuine engine-level coverage; both are disclosed rather than falsely claimed as tested. Mobile/tablet coverage IS genuine, via Playwright device-viewport emulation (iPhone 13 / iPad profiles) across Settings, Chat, Image Gen, and the Ninja Sales leads list — not skipped, not simulated after the fact.
- [x] **Follow-up: genuine WebKit-engine pass (not just Chromium)**: the line above undersold what's actually possible on Windows — Playwright ships its own WebKit browser build, the real WebKit rendering engine (same family Safari uses), running natively here. Installed it (`npx playwright install webkit`) and drove Login → Dashboard, Settings (desktop + 390px mobile), Ninja Chat (real message sent, real response rendered), Image Gen, and the public Pricing section under `webkit`, screenshotting each and diffing against Chromium. Four of five surfaces matched Chromium pixel-for-pixel in layout (the `grid-cols-1` Settings-mobile fix holds under WebKit's grid engine too — zero overflow at 390px). One real, visually-confirmed difference found: headings/prices/buttons using Tailwind's default `font-bold`/`font-semibold` on the app's fallback font stack (`Inter` → `system-ui` → `sans-serif` — "Inter" is named in `tailwind.config.js` but never actually loaded via `@font-face`/package) render at a visibly lighter weight under WebKit than Chromium, most visible on the Pricing page's headline and plan prices. No CSS authoring bug found — the fallback chain is written correctly, and it did not reproduce on Settings/Chat page titles. Leading theory: a font-synthesis (fake-bold) difference between engines for the resolved fallback font on this Windows box, possibly specific to Playwright's WebKit-on-Windows font access rather than something that reproduces on real macOS Safari (genuine bold San Francisco instance for `system-ui` there). Deliberately not fixed — reported with screenshots rather than risking a blind global font-stack change under time pressure. Secondary, lower-severity note: the dashboard's first paint after login logged several WebKit-only console `pageerror`s ("... due to access control checks") for a batch of dashboard-widget fetches that Chromium's console showed none of; a second dashboard visit in the same WebKit session rendered the same widgets with real data, so this looks like noisy/duplicate requests failing under WebKit's stricter CORS enforcement rather than a full data-loading break — flagged for backend CORS review, not blocking.

- [x] **Follow-up: domain/SSL provisioning genuinely proven end-to-end**, closing the item below — real DNS/a real customer domain still weren't available in this environment, but Let's Encrypt's own official test infrastructure was: Pebble (the real test ACME server Let's Encrypt and certbot's own test suites use) plus its `challtestsrv` DNS-mocking companion, run in an isolated, purpose-built Docker Compose stack (`docker-compose.ssl-test.yml`, doesn't touch `docker-compose.dev.yml`/`docker-compose.prod.yml`, no container-name or port collisions with anything else running) that exercises the **real, unmodified** pipeline code — the actual `certbot/provision-domains.sh`, the actual `nginx-proxy/nginx.conf` catch-all ACME-challenge block, the actual `domainSslService.js` queue-marker mechanism — not a rewrite standing in for it. Result: a real certificate was genuinely issued (`Successfully received certificate`, correct issuer chain, correct SAN) for a test domain, the real `write_server_block()`/`reload_nginx()` logic ran and produced a syntactically valid nginx config (`nginx -t` passed), and the stack was torn down cleanly afterward with the pre-existing dev containers (`startup-ninja-*-dev`, unrelated `talimai_*`) confirmed untouched throughout. This proves the real certbot/nginx/webroot/reload mechanics work correctly together — it does not prove real production Let's Encrypt rate limits/behavior or real public DNS propagation, which genuinely need a real domain to test and stay honestly out of reach of any dev-environment session.
- [x] **Follow-up: Web Builder template count raised from 20 to 28**, closing the item below — 8 new templates (nonprofit, event-planning, interior-design, photography, dental, veterinary, financial-advisor, moving-storage — all genuinely distinct from the existing 20's industries) added through the exact same seeding mechanism the prior 11 used, each a complete multi-section GrapesJS project matching the existing templates' depth, not a shallow stub. Verified live: reseeded the real local dev database, confirmed via the real `GET /templates` endpoint that all 28 appear correctly, and confirmed one of the new templates renders and selects correctly in the real in-editor template picker.
- [x] **Follow-up: a real, previously-undiscovered usage-counter bug found and fixed while verifying the template work.** The agent adding templates found the demo account's website-creation quota exhausted (54/10) despite having **zero** actual websites — investigated rather than just resetting the number, and found the real cause: `website_creation` and `team_members` are "current active count" style limits (feedback.md's own plan descriptions say "Up to N active hosted websites"/"Up to N users"), but their usage was tracked via a Redis counter (`checkUsageLimit`'s `incrby`) that only ever increments — nothing anywhere in the codebase ever decremented it when a website was deleted or a team member removed, so both counters could only ever climb toward the limit and get permanently stuck there, regardless of how much real capacity a user actually freed up. This is exactly the class of bug feedback.md's P0 "remove every wrong/stale number" concern is about, just not one the original audit's phrasing happened to catch. Fixed with a new `POST /user/release-usage` endpoint (auth-service) that decrements a usage counter (floored at 0, never goes negative), called from `website-builder-service`'s `deleteWebsite` and `admin-service`'s `deleteMember` after a successful delete. A second, adjacent bug was caught in the same area and fixed: website creation incremented usage *before* validating the request body, so a request that was always going to fail (e.g. a missing title) still silently consumed a real website-creation slot — reordered to validate first, and added a release-on-save-failure safety net for the rarer case where validation passes but the actual database save fails. **Verified live end-to-end** against the real running services: confirmed an invalid create no longer increments usage, confirmed a valid create correctly moves usage 0→1, confirmed deleting that website correctly moves it back 1→0, and corrected the demo account's actual stale counters (54→0 for websites, 7→0 for team members, both now matching its real 0-and-0 counts). **Deliberately not extended** to `sales_leads`/`sales_projects`/`legal_contracts`/`facebook_page_connect`/`website_hosting` in this same pass — those may or may not share this exact "active count" semantic (some could legitimately be "created this cycle" caps by design) and each needs its own real delete-path trace before assuming the same fix applies, rather than guessing.

**Disclosed remaining scope**: a true Safari/macOS pass — real ITP behavior, Apple-specific extensions, macOS-only rendering quirks, real Apple hardware — is still out of reach of this Windows dev environment even with genuine WebKit-engine coverage now in place (see above), and would need a non-Windows environment or a cross-browser cloud service before launch if Safari users are a meaningful share of traffic; the WebKit pass's one real finding (a font-weight rendering difference on the Pricing page, ambiguous root cause) was deliberately left unfixed rather than risking a blind global font-stack change under time pressure; the Web Builder whole-page ("nothing selected") AI edit path now has one automatic retry at a lower temperature for its known LLM-reliability failure mode, which should reduce but can't fully eliminate the underlying non-determinism; and the usage-counter-never-decrements bug class above was fixed only where directly evidenced (`website_creation`, `team_members`) — the other resource-style limits listed above should get the same real-delete-path trace before launch, not assumed fixed by association.

---

## Sequencing rationale

Phase 0 first because it's cheap to fix now and expensive to leave broken while building on top of it (sessions dying mid-work will corrupt testing of every later phase; unfixed feature-gating means Phase 1-6 UI has nothing real to enforce against). Phases 1-5 are ordered by `feedback.md`'s own P0 numbering, adjusted only where there's a hard dependency (Phase 4 domains needs Phase 3's publish step to exist first). Phase 6 (P1) is explicitly sequenced after all P0 work, per `feedback.md`'s own instruction not to spend launch week on unrelated features. Phase 7 (security) runs continuously alongside 1-6 rather than being saved for the end. Phase 8 is the launch gate.
