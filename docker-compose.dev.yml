version: "3.8"

services:
  # ============================================================================
  # FRONTEND (DEV)
  # ============================================================================
  
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL}
        VITE_WEB_BUILDER_SERVICE_URL: ${VITE_WEB_BUILDER_SERVICE_URL}
        VITE_IMAGINATIVE_SERVICE_URL: ${VITE_IMAGINATIVE_SERVICE_URL}
        VITE_WEBSITE_BUILDER_LICENSE_KEY: ${VITE_WEBSITE_BUILDER_LICENSE_KEY}
        VITE_SYSTEM_BASE_DOMAIN: ${VITE_SYSTEM_BASE_DOMAIN}
        VITE_SYSTEM_IP: ${VITE_SYSTEM_IP}
        VITE_STRIPE_PUBLISHABLE_KEY: ${VITE_STRIPE_PUBLISHABLE_KEY}
        VITE_GOOGLE_CLIENT_ID: ${VITE_GOOGLE_CLIENT_ID}
        VITE_MICROSOFT_CLIENT_ID: ${VITE_MICROSOFT_CLIENT_ID}
        VITE_MICROSOFT_TENANT_ID: ${VITE_MICROSOFT_TENANT_ID}
        VITE_MICROSOFT_REDIRECT_URI: ${VITE_MICROSOFT_REDIRECT_URI}
        VITE_GOOGLE_FONTS_API_KEY: ${VITE_GOOGLE_FONTS_API_KEY}
    container_name: startup-ninja-frontend-dev
    restart: always
    # Ports removed - accessed via nginx-proxy (backend network)
    environment:
      - VITE_API_BASE_URL=/api
      - VITE_SYSTEM_IP=54.159.113.115
    networks:
      - backend_shared-network

# ============================================================================
# NETWORKS
# ============================================================================

networks:
  backend_shared-network:
    external: true
    name: backend_shared-network
